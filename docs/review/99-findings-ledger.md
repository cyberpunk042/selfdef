# Findings ledger

> Master index of every finding raised in Phase 1. Per-area
> findings (`M-`, `C-`, `I-`, `D-`, `T-`, `R-`, `S-`) are
> consolidated here as canonical `F-2026-NNN` ids.
>
> Each row carries the canonical id, the severity, the surface,
> a one-line summary, and a recommended **next phase** label:
>
> - `investigate` — root cause not fully understood yet; Phase
>   2 reads code / runs experiments.
> - `design` — root cause is clear; Phase 2 produces an SDD.
> - `implement` — root cause is clear and the fix is small;
>   Phase 2 implements directly with a short rationale doc.
> - `doc` — pure documentation drift; Phase 2 updates the
>   relevant doc file.
>
> Severity:
> - `blocker` — the artifact's central promise fails for the
>   documented use case.
> - `important` — works but a documented promise isn't honoured.
> - `nice` — polish.
> - `SDD-debt` — undocumented design decision with downstream
>   consequences.

---

## Blocker findings (5)

| id | severity | surface | summary | next phase |
| --- | --- | --- | --- | --- |
| F-2026-001 | blocker | `crates/selfdef-collector-tetragon/src/lib.rs` | Every Tetragon event hardcodes `SeverityId::Informational`. Policy `selfdef.io/severity` annotation is ignored. *(was I-007)* | design — **closed** by SDD-001 implementation PR (collector now attaches structured `raw.tetragon.{policy_name,policy_namespace,action,function_name}`; severity stays Informational at the collector layer per the SDD's "collectors are dumb translators" invariant — promotion happens in the sigma rule below) |
| F-2026-002 | blocker | `rules/sigma/` | No correlator rule promotes Tetragon agent-guard events to `category_uid = Findings`. Responder never fires `NotifyAction`. *(was I-008)* | design — **closed** by SDD-001 implementation PR (`rules/sigma/hardening/agent_guard_violation.yml` matches `raw.tetragon.policy_name|startswith "selfdef-agent-"` + `action|in [Sigkill, Override, NotifyKiller]`, promotes to `level: high`; corpus covers Sigkill / NotifyKiller / Post-negative / non-agent-guard-negative / non-tetragon-source-negative) |
| F-2026-003 | blocker | `modules/tetragon/README.md` + default profile comment | README points at the wrong collector (`[collectors.eventstream]` instead of `[collectors.tetragon]`). Events silently dropped. *(was I-006)* | doc — **closed** by SDD-001 implementation PR (`modules/tetragon/README.md` now names `[collectors.tetragon]` and ships a paste-ready snippet; reciprocal note on the eventstream collector being wrong is explicit) |
| F-2026-004 | blocker | `selfdef-config EventstreamConfig::default` + `ApiConfig::default` | Default daemon config disables every collector and the API. Modules' "out of the box" promises (drift→notifier, Prometheus scrape) require manual bridging the documentation doesn't surface. *(was I-002, I-004)* | design — **closed** by SDD-002 implementation PR (the bridge is now a manifest-level contract: every active module's `[daemon_requires]` is checked against `/etc/selfdef/selfdef.toml` before any apply.sh fires; mismatch prints a copy-pasteable snippet and exits 2 unless `--ignore-daemon-requires` is set) |
| F-2026-005 | blocker | `modules/vpn-bridge/install/profiles/*.sh` | `instanced = true` declared in manifest; profile scripts use hard-coded state paths. Multi-instance host silently corrupts state. *(was M-008, S-007)* | design — **closed** by SDD-003 implementation PR (manifest now declares per-profile `instanced` capability via `[profiles.details.<name>]`; resolver refuses `vpn-bridge#<inst>` for singleton profiles before any script runs; `relay-via-server` parameterises iface / nftables table / nftables state-file by `${SELFDEF_INSTANCE_ID}`; `tailscale` + `cloudflare-tunnel` `die` defence-in-depth when the resolver is bypassed) |
| F-2026-006 | blocker | daemon integration tests | No AI-machine end-to-end test (policy fires → operator alert). F-2026-001 + F-2026-002 would have been caught by such a test. *(was T-008)* | implement — **closed** by SDD-001 implementation PR (`crates/selfdef-daemon/tests/m_ai_machine.rs` runs Tetragon JSON through collector + correlator and asserts a Detection Finding lands with severity High; negative-case Post line is rejected) |

---

## Important findings (22)

| id | severity | surface | summary | next phase |
| --- | --- | --- | --- | --- |
| F-2026-010 | important | `modules/agent-guard/install/apply.sh` | tetragon `policy_dir` discovery depends on `SELFDEF_TETRAGON_CONFIG`; silent fallback to hard-coded path. *(was M-002)* | design — **closed**: the fallback is no longer silent. When tetragon's config is unreadable, apply.sh now emits a `WARN: tetragon config not readable … assuming policy_dir=/etc/tetragon/tetragon.tp.d …` line telling the operator to apply tetragon first or set `SELFDEF_TETRAGON_CONFIG`, so a non-default tracing-policy-dir can't silently strand agent-guard's TracingPolicies where Tetragon never loads them. Locked by `L2-agent-guard.bats` "loudly warns + falls back …" (asserts the WARN + the default path on an unreadable config). |
| F-2026-011 | important | `modules/tetragon/install/apply.sh` | `event_log_path` alignment with daemon eventstream is operator-managed; no apply-time surface or check. *(was M-004)* | design — **closed** (stale premise): an apply-time check DOES exist — the `[daemon_requires]` pre-flight (SDD-002, `check_daemon_requirement`, bypass `--ignore-daemon-requires`; mechanism tested in `cli_modules_daemon_requires.rs`). tetragon `module.toml` declares `"collectors.tetragon.input_path" = "${event_log_path}"` — `collectors.tetragon` (not eventstream) is the correct sink for Tetragon's native JSON, so the finding's "eventstream" framing was a misread. P4 lock added: `module_tetragon.rs::tetragon_declares_event_log_path_daemon_requires_alignment` asserts the alignment so the pre-flight keeps protecting `event_log_path`. |
| F-2026-012 | important | `modules/observability/` | Three different defaults for `scrape_targets` across README, both profile files, apply.sh fallback. *(was M-005)* | doc — **closed** by doc-sweep PR |
| F-2026-013 | important | `modules/observability/README.md` | Dashboard depends on selfdef-daemon `/metrics`; README only mentions Tetragon. *(was M-006)* | doc — **closed** by doc-sweep PR |
| F-2026-014 | important | every module using the event bus | No `depends_on = ["detect-host"]` declared; consume-side of detect-host's `provides = ["event-bus"]` is silent. *(was M-009)* | design |
| F-2026-015 | important | `selfdef-config CorrelatorConfig::window_secs / threshold` | Dead config knobs — exposed but never read. Sigma rules carry their own. *(was C-002)* | implement — **closed** by dead-knob cleanup PR (rustdoc-marked vestigial; commented out in `selfdef.toml.example`) |
| F-2026-016 | important | `selfdef-config StoreConfig::hot_retention_days` + `selfdef-store` | Dead knob — retention horizon exposed but no sweeper enforces it. *(was C-003)* | design — **closed** by SDD-081 (hot-store retention sweep): `selfdef-store::SqliteStore::prune_older_than` deletes events past the horizon; `selfdef-daemon::retention_sweep_loop` runs an initial sweep at startup + every 6h, computing `cutoff = now - hot_retention_days` and logging pruned/remaining counts. `hot_retention_days=0` = explicit opt-out (keep forever). Tests: store prune (idempotent) + daemon `sweep_once` (prunes past horizon / keeps fresh). |
| F-2026-017 | important | `selfdef-store/src/sqlite.rs:106` | Duplicate `const SCHEMA_VERSION: u32 = 1` instead of referencing `selfdef_core::SCHEMA_VERSION`. *(was C-005)* | implement — **closed** by dead-knob cleanup PR (now imports `selfdef_core::SCHEMA_VERSION`) |
| F-2026-018 | important | `modules/integrity-sentinel/profiles/*.toml` | `event_stream_path` commented out in shipped profiles. *(was I-001)* | implement — **closed** by SDD-002 implementation PR (both strict.toml and warn-only.toml ship with the path live by default; the manifest's `[daemon_requires]` ensures the daemon-side collector path is set before apply proceeds) |
| F-2026-019 | important | `modules/observability/README.md` | Prometheus bearer-token auth on TCP `/metrics` is undocumented. Scrape will 401. *(was I-005)* | doc — **closed** by doc-sweep PR |
| F-2026-020 | important | repo-wide (packaging + module READMEs) | No bundled `selfdef.toml` template for "three modules with sane defaults". *(was I-009)* | design — **closed** by SDD-002 implementation PR (defaults bridge is now per-module via `[daemon_requires]` + `selfdefctl modules show-requires`; bundled templates can ship later as syntactic sugar over the manifest contract per SDD-002's appendix) |
| F-2026-021 | important | `README.md` line 7 | "Milestone 1 — Scaffolding only" status banner is false. *(was D-001)* | doc — **closed** by doc-sweep PR |
| F-2026-022 | important | `ARCHITECTURE.md` | `/metrics` endpoint and the module catalog (AI-machine track) missing from the architecture description. *(was D-003, D-004)* | doc — **closed** by doc-sweep PR |
| F-2026-023 | important | `SECURITY.md` (and mirror) | `/metrics` endpoint not in threat model. Token rotation lifecycle unspecified. *(was D-005, S-002)* | design — **closed** by SDD-004 implementation PR (`/metrics` row in Assets table; API surface mitigations cover transport / read-cap / token-rotation lifecycle; metrics-token rotation tracked as a Known gap follow-up) |
| F-2026-024 | important | `SECURITY.md` | `/etc/tetragon/tetragon.tp.d/` writable directory not in threat model — eBPF policy injection vector. *(was D-006, S-001)* | design — **closed** by SDD-004 implementation PR (TracingPolicy directory row in Assets table; Policy surface mitigations cover ownership + integrity-sentinel baseline; signing tracked as a Known gap follow-up) |
| F-2026-025 | important | `SECURITY.md` | Pod-label scope reliance on k8s label RBAC not in threat model. *(was D-007, S-003)* | design — **closed** by SDD-004 implementation PR (Adversary class 6 "Cluster-tenant attacker"; Policy surface mitigations document the required RBAC posture; cluster control plane added to Trust assumptions) |
| F-2026-026 | important | `SECURITY.md` | Eventstream JSONL paths are a trust boundary; no documented ownership / mode requirements. *(was S-004)* | design — **closed** by SDD-004 implementation PR (Eventstream JSONL row in Assets table; Policy surface mitigations enumerate the per-path trust posture; daemon-side ownership/mode check at parse time tracked as a Known gap follow-up) |
| F-2026-027 | important | `docs/src/{dev,ops}/*.md` | Five stub pages linked from `SUMMARY.md` are one-line TODOs. *(was D-009)* | doc — **closed** by doc-sweep PR |
| F-2026-028 | important | `docs/api.md`, `docs/ebpf.md`, `docs/nats.md`, `docs/ssh-wrap-install.md` | Real operational guides orphaned from mdbook outline. *(was D-010)* | doc — **closed** by doc-sweep PR (files moved into `docs/src/{ops,dev}/`) |
| F-2026-029 | important | `docs/src/modules.md:202-207` | Stale claim that only `detect-host` ships. *(was D-011)* | doc — **closed** by doc-sweep PR |
| F-2026-030 | important | every `module_*.rs` integration test | Dry-run mode not negatively asserted — a regression making `SELFDEF_DRY_RUN=1` mutate state would pass every existing test. *(was T-003)* | implement — **fully closed** by SDD-005 reference (`module_vpn_bridge.rs`) + the 7-module dry-run-noop migration follow-up PR (agent-guard, bridge-l2, integrity-sentinel, observability, polarproxy, suricata, tetragon all now carry a `dry_run_apply_must_be_a_noop_on_disk` test using the shared `snapshot_tree` + `assert_tree_unchanged` helpers) |
| F-2026-031 | important | `m12_api.rs:469-482` | `/metrics` content-type and exposition body checks too loose. *(was T-005)* | implement — **closed** by SDD-005 implementation PR (new strict `mod prom` parser + `metrics_exposition_passes_format_strict_parse` test) |
| F-2026-032 | important | `m12_api.rs` | No test that `/metrics` is accessible with a Read-only token. *(was T-006)* | implement — **closed** by SDD-005 implementation PR (`metrics_allows_read_capability` test) |
| F-2026-033 | important | `selfdef-correlator/tests/` | No SIGHUP-while-processing test. Hot-reload claim from ARCHITECTURE.md is unverified under traffic. *(was T-007)* | implement — **closed** by SDD-005 implementation PR (`hot_reload.rs::correlator_swaps_rules_atomically_under_live_traffic` + `..._keeps_prior_set_on_parse_failure`) |
| F-2026-034 | important | `selfdef-store` | No concurrent-insert or crash-recovery test. *(was T-009)* | implement — **closed** by SDD-005 implementation PR (`tests/concurrent.rs` × 3 tests covering both contracts) |
| F-2026-035 | important | `selfdef-nats` | No real-broker round-trip test. JetStream durability promises unverified. *(was T-010)* | implement — **closed** by SDD-005 implementation PR (`tests/integration.rs` spawns a real `nats-server`; `#[ignore]`-gated so CI without the binary stays green) |
| F-2026-036 | important | `selfdef-collector-tetragon` | No isolation test; every translation goes through daemon tests only. *(was T-011)* | implement — **closed** by SDD-005 implementation PR (`tests/translation.rs` × 10 tests covering every translation branch plus 3 tolerance branches; new `pub fn translate_line` surface) |
| F-2026-037 | important | CHANGELOG + PR descriptions for #21, #22, #24 | Operator outcomes are described as if end-to-end; audit reveals they aren't. *(was R-003)* | doc — **closed** by doc-sweep PR via a CHANGELOG "Honest correction" entry pointing at SDD-001 |

---

## Nice findings (12)

| id | severity | surface | summary | next phase |
| --- | --- | --- | --- | --- |
| F-2026-050 | nice | `modules/agent-guard/install/uninstall.sh` | Hand-enumerated policy list; will drift if a sixth policy is added. *(was M-001)* | implement — **closed** by SDD-006 v2 follow-up PR (`packaging/lib/module-lib.sh` bumped to v2; new `module_record_file` / `module_render_files` / `module_clear_manifest` helpers; agent-guard's apply.sh records every rendered file into a per-module manifest at `/var/lib/selfdef/installed/agent-guard.manifest`; uninstall.sh iterates the manifest and falls back to the legacy enum for pre-v2 installs) |
| F-2026-051 | nice | `modules/agent-guard/install/lib.sh render_pod_scope` | Awk state machine fragile against future policy selectors. *(was M-003)* | implement — **partial close** by SDD-006 implementation PR (shared lib v1 doesn't ship YAML-aware editing; a v2 with `yq`/python helpers would close fully) |
| F-2026-052 | nice | `modules/observability/assets/dashboards/` | Hardcoded Tetragon metric name `tetragon_msg_sigkill_total` without an upstream-version pin. *(was M-007)* | investigate — **closed** by SDD-079 (Tetragon metric-name contract): the four pinned series now live in `modules/observability/assets/contracts/tetragon-metrics.toml` with a `verified_tetragon_version` window; CI test `tests/observability/test_tetragon_metric_name_contract.py` locks dashboard↔contract↔SDD↔README in lockstep; `check.sh` gained an opt-in warn-only live-endpoint series probe turning a silent flat panel into a loud warning. Fail-closed binary-version enforcement from the tetragon `requires` block remains a Phase-3 follow-up (SDD-079 D-1). |
| F-2026-053 | nice | `selfdef-config/src/lib.rs BusConfig::backend` | Dead knob — always "inproc", daemon doesn't branch on it. *(was C-001)* | implement — **closed** by dead-knob cleanup PR (rustdoc-marked vestigial; commented out in `selfdef.toml.example`) |
| F-2026-054 | nice | `selfdef-daemon/src/main.rs build_notifier_chain` | A `[notifier.ntfy]` block with no matching `"ntfy"` in `channels` silently disables ntfy without a startup warning. *(was C-004)* | implement — **closed** by follow-ups cleanup PR (warns at startup for either `ntfy` or `signal` orphan block) |
| F-2026-055 | nice | `selfdefctl events emit` | `--out` has no default. Callers must hard-code the path. *(was I-003)* | design — **closed**: `--out` now defaults to the per-producer CLI emission file `/var/lib/selfdef/eventstream/cli-emit.jsonl` (matches the eventstream collector's per-producer JSONL convention); operators add that one path to `[collectors.eventstream].paths` and ad-hoc `selfdefctl events emit` works with no flag. |
| F-2026-056 | nice | `README.md` | No catalog of shipped modules in the repo-root README. *(was D-002)* | doc — **closed** by doc-sweep PR #30 (README now ships a Module catalog table) |
| F-2026-057 | nice | `CHANGELOG.md` PR #22 entry | Observability scope ("we configure, we don't install") implicit; should be explicit. *(was D-008)* | doc — **closed** by doc-sweep PR #30 (CHANGELOG "Honest correction" entry) |
| F-2026-058 | nice | `modules/vpn-bridge/README.md` | No mention of multi-instance corruption risk (F-2026-005). *(was D-012)* | doc — **closed** by nice-cluster PR (README now carries a Multi-instance caveat block referencing SDD-003) |
| F-2026-059 | nice | `panic` subcommand vs `modules uninstall` | Hostname-confirm validation duplicated; could share a helper. *(was R-001)* | implement — **closed** by follow-ups cleanup PR (`check_confirm_hostname` + `ConfirmRefusal` helper extracted; both call sites use it) |
| F-2026-060 | nice | `crates/selfdef-cli/tests/*` | Helper functions duplicated across 9 test files; refactor to `tests/common/mod.rs`. *(was T-001)* | implement — **CLOSED** (verified 2026-06-08): `tests/common/mod.rs` holds all 7 heavy helpers (workspace_root / write_file / last_stdout_line / snapshot_tree / assert_tree_unchanged / write_executable / prepended_path); all 17 `module_*.rs` declare `mod common;` and consume them — **0** local re-definitions of any heavy helper remain (the only per-file symbol is the idiomatic 3-line `module_dir()` slug-wrapper delegating to `common::module_dir("<slug>")`, which is design, not debt). Locked by `scripts/test/L1-test-helper-dedup-coherence.sh` (wired into coherence): fails if any `module_*.rs` drops `mod common;` or re-defines a heavy helper, so the dedup can't regress by copy-paste. |
| F-2026-061 | nice | `cli_modules_apply.rs:118` and similar | Substring assertions on `Summary:` lines brittle against cosmetic changes. *(was T-002)* | implement — **partial close** by follow-ups cleanup PR (`m12_api.rs` `/metrics` test now exact-matches the Content-Type, asserts each TYPE line is unique, and validates the exposition-format shape line by line; the `Summary:` substring assertions are still in the module tests and are a follow-up). **Module-test follow-up now closed**: the partial `contains("Summary: N ok")` assertions in `cli_modules_apply.rs` + `cli_modules_uninstall.rs` (which would pass even with a wrong skipped/failed count) are tightened to the full `"N ok, M skipped, K failed"` line, so a cosmetic-or-substantive change to either of the other two counts is now caught. 44 tests pass. |
| F-2026-062 | nice | per-module test suites | Idempotent-reapply coverage is tetragon-only. *(was T-004)* | implement — **partial close** by follow-ups cleanup PR (agent-guard byte-stable reapply test) + the structural `L2-module-apply-idempotency-guard.bats` (variant-A/B sweep across all 187 apply.sh). The 4 named modules (bridge-l2/suricata/polarproxy/vpn-bridge) `die` at preflight without `suricata`/`nft`/`systemctl` so a full runtime reapply isn't feasible in the L2 sandbox; instead their **render paths** are now contract-tested directly for ALL FOUR: suricata `nfqueue.rule.tmpl`, bridge-l2 `nftables.conf.tmpl` (3 tokens; also fixed a stale `@@MGMT_IFACE@@` doc-comment that survived every render), polarproxy `polarproxy.service.tmpl` (6 tokens) + `nat-redirect.rule.tmpl`, vpn-bridge `forward.rule.tmpl` (3 tokens). Each suite mirrors apply.sh's sed verbatim and asserts: no surviving `@@TOKEN@@`, concrete values land, byte-stable across renders, and a doc/substitution token-set drift guard. **Closed** (render-contract mechanism; runtime reapply remains binary-gated and out of L2-sandbox scope). |
| F-2026-063 | nice | `selfdef-responder` | No isolation test for action dispatch / dry-run / unknown action. *(was T-012)* | implement — **closed**: `crates/selfdef-responder/tests/dispatch_isolation.rs` drives `Responder` through its public API with a recording mock `Action` — 5 tests: dispatch_single runs a known action (live mode propagated), unknown action → None (nothing runs), dry-run flag reaches the action + DryRun outcome, fire() executes an allowlisted action, fire() never runs a disallowed one. |
| F-2026-064 | nice | `rules/sigma/` + `tests/replay/` | No audit of rule ↔ corpus coverage. *(was T-013)* | implement — **closed**: `tests/replay/test_rule_corpus_coverage.py` (stdlib-only, no yaml dep) audits corpus integrity — every `rule_id` a `.expected.yaml` references is a real rule in `rules/sigma/` (catches dead/typo'd assertions), every `.jsonl` is valid JSON-lines, every `.expected.yaml` has its sibling `.jsonl`, and the corpus is non-vacuous. 4 tests; complements the coherence harness's per-rule `.tests.yaml` pairing on the corpus side. |
| F-2026-065 | nice | `selfdefctl events emit` (security view) | Event-injection primitive; sub-case of F-2026-026. *(was S-005)* | doc — **closed** by follow-ups cleanup PR (Known gaps entry in SECURITY.md + mirror in `docs/src/security.md`) |
| F-2026-066 | nice | `SECURITY.md` | `/metrics` daemon-uptime gauge enables credential-file timing chains. *(was S-006)* | doc — **closed** by follow-ups cleanup PR (Known gaps entry in SECURITY.md + mirror in `docs/src/security.md`) |

---

## SDD-debt findings (4)

| id | severity | surface | summary | next phase |
| --- | --- | --- | --- | --- |
| F-2026-080 | SDD-debt | `modules/observability/assets/dashboards/` | Dashboard implicitly depends on agent-guard policies firing — no manifest-level coupling captured. *(was M-010)* | design — **closed** (documented as a deliberate runtime coupling, not manifest debt): observability README § "Policy-data coupling (F-2026-080)" explains the Tetragon kills-by-policy + map-errors panels only carry data once a policy module (agent-guard / perimeter) is applied + firing — an empty panel on a policy-less host is correct, not a fault. `observability` deliberately does NOT `depends_on` `agent-guard` because the dashboard is useful (event rate, process cache, daemon panels) with zero policy modules; the coupling is soft/runtime, and operators wanting the kill-by-policy view apply a policy module. |
| F-2026-081 | SDD-debt | `modules/*/install/lib.sh` | Duplicated helpers (`toml_get`, `run`, `log`, `emit_status`) across every module. *(was M-011)* | design — **closed** by SDD-006 implementation PR (shared `packaging/lib/module-lib.sh`; eight modules migrated byte-stably; dispatcher exports `SELFDEF_MODULE_LIB` with workspace+system fallback; version-pinned at `SELFDEF_MODULE_LIB_VERSION=1` with mismatch refused at source time) |
| F-2026-082 | SDD-debt | PR-author discipline | Tests verify the unit, not the flow. Need a design doc on what "integration-tested" means at the daemon ↔ module seam. *(was R-002)* | design — **closed** by SDD-005 implementation PR (`docs/dev/test-contract.md` runbook documents the four categories + three shared patterns; SDD-005 itself flips to `implemented`) |
| F-2026-083 | SDD-debt | development cadence | Module PRs introducing new event sources should be preceded by collector/correlator prep PRs. *(was R-004)* | design |
| F-2026-084 | nice | `modules/observability/assets/alerts/selfdef.yml.template` | `selfdef_ingest_lag_events_total` (events dropped when the metrics-ingest subscriber is over-subscribed → every counter-based alert silently under-counts) is now dashboarded (panel id=132, SD-API health row) but has no **alert**. A warning alert is warranted — it's meta-observability (the observability itself degrading). Deferred because doing it right is a cross-repo change: the alert needs a real `runbook_url`, and no `wiki/runbooks/metrics-ingest-lag.md` exists in the info-hub yet. Pair the selfdef alert rule with a new info-hub runbook (proper producer/runbook-host responsibilities) in one coordinated change. | design **CLOSED**: shipped the `SelfdefMetricsIngestLag` warning alert (selfdef-meta-observability group, `increase(selfdef_ingest_lag_events_total[10m]) > 0`) paired with the new info-hub runbook `wiki/runbooks/metrics-ingest-lag.md` (pipeline post PASS) — the coordinated producer/runbook-host change. L1-prometheus-alerts (incl. runbook-existence Gate 3b) passes; README updated 15→16 rules / 3→4 groups. |
| F-2026-085 | important | `crates/selfdef-cli/src/modules.rs` (apply path) | Module `requires` of `kind = "binary"` / `"package"` / `"kernel-feature"` is **display-only** — `selfdefctl modules show` prints it (modules.rs:1325) but nothing host-evaluates it before apply (only `requires_hardware` is evaluated via `.evaluate(c)`). So each apply.sh re-implements an ad-hoc `command -v <bin> ... \|\| die`, and a missing binary is only discovered mid-apply (half-applied state). `L1-module-requires-binary-coherence.sh` now guarantees the declared binary `requires` is **complete** vs the `\|\| die` checks; the enforcement half is open: add a central `requires` pre-flight in the apply path that host-checks each `kind="binary"` require with `command -v` (and packages via the package manager) and refuses to start apply when unmet, with `--ignore-module-requires` to override (mirrors the existing `--ignore-daemon-requires`). Needs a central requires-enforcement policy decision (PO). | design |
| F-2026-086 | nice | `modules/{bridge-l2,polarproxy,suricata,vpn-bridge}/profiles/*.toml` | **Documented-but-unread module-profile keys (phantom config).** An operator-facing-key dead-config sweep (every `profiles/*.toml` key vs `toml_get` reads across the module's apply/check/lib/profile-scripts/templates) found 5 keys documented in a module profile that the OWNING module reads nowhere: `bridge-l2.persist`, `polarproxy.bridge_name` (actually read only by bridge-l2), `suricata.enable_collector` + `suricata.interfaces` (suricata apply.sh reads only `mode`/`queue_num`; interface config is suricata.yaml's domain), `vpn-bridge.listen_port` (read only by polarproxy). An operator who sets these expecting an effect gets a silent no-op. The cross-module reads (bridge_name, listen_port) look like copy-pasted profile templates. Fix is **per-key intent-dependent** — each is either (a) remove the phantom key + clarify the comment (if the behavior is owned elsewhere, e.g. suricata.yaml), or (b) wire it into the module's apply.sh (if the profile-key WAS meant to drive it). Not auto-fixed: removing operator-settable keys / inferring intent across 4 modules is a design call. Parallels the closed `[correlator].hot_reload` phantom (9e1c4ed) and `[daemon].log_format` dead-config (5f657bd) on the daemon side. | investigate |
| F-2026-088 | nice | `crates/selfdef-daemon/src/main.rs` (notifier channel build) vs `crates/selfdef-config` validate() | **`selfdefd --validate` passes a config whose alert channel will silently never start.** Each alert integration (loki/opensearch/thehive/pagerduty/webhook) **correctly enforces** its documented `Must be https://` contract at build time (e.g. `selfdef-integration-loki/src/lib.rs:145` `if !endpoint.starts_with("https://") { Err(EndpointNotHttps) }`, with a `plaintext endpoint must fail` test) — so the https guard itself is **verified sound, not a gap**. The observation is the *seam*: a plaintext-`http://` endpoint (a typo, or a copy-paste from a non-TLS internal example) passes `Config::validate()` (which has **no** channel-scheme/buildability preflight) and `selfdefd --validate`, then at daemon start the channel build fails and main.rs logs `"<chan> channel skipped (config rejected by builder)"` at **warn** and the daemon **continues** (main.rs:1255 loki, and the identical pattern for all 10+ channels). An operator watching only for a clean `--validate` + a running daemon believes findings are shipping to Loki; they are not. This is the **same failure-class** the api transport preflight (config validate lines 364-388) was added to catch ("the task only logs … while the daemon keeps running — so a misconfigured api silently doesn't serve … fail fast instead"). **NOT auto-fixed — design/PO-tier**: the log-and-continue is a *deliberate, consistent* resilience choice (every channel has the explicit skip message) — a typo'd **optional alert** URL hard-failing the whole security daemon's detection would be a worse failure than dropping one channel, so this is NOT a clear bug to flip. The in-bounds improvement (deferred for operator intent) is **observability, not hard-fail**: have `--validate` emit per-channel **warnings** for configured-but-unbuildable channels (https scheme, missing auth_token_file, opensearch empty index) without failing the daemon — closing the "—validate green yet channel dead" gap while preserving the resilience policy. Mirrors the api-preflight rationale but as warnings for optional channels. | design | (`crates/selfdef-*-backend`) + sovereign-os `scripts/cockpit/*queue*.py` | **CORRECTED 2026-06-09 (this row first mis-framed blockset as uniquely broken — deeper trace overturned that).** The selfdef enforcement primitives follow the **paired-enforcement-primitive five-milestone architecture**: MS1 ships an `InMemoryBackend`; the production adapter (MS1b — nftables-set / cgroupv2-freezer, the FS-persistent path) is feature-gated and **lands separately** (decision: info-hub `wiki/decisions/01_drafts/in-memory-backend-as-ms1-substrate.md`, SDD-065/066). Verified production wiring 2026-06-09: (a) the daemon responder action vec (`selfdef-daemon/src/main.rs:628`) wires only the 7 CLASSIC actions — **none** of the SDD-065+ enforcement actions (BlockIp / MountBindingUnbind / Quarantine / …); (b) the CLI (`selfdef-cli/src/main.rs:3294+`) constructs `InMemoryBackend::new()` for EVERY enforcement primitive ("MS3 default backend = InMemoryBackend"); (c) the `FsBackend` variants (`FsBackend::open`, `write_atomic("pending-*.json")`) exist in ~12 backend crates but are referenced **only in tests** — `grep 'FsBackend::open' (non-test)` = 0. **Consequence**: ALL 14 cockpit pending-queue cards (`card_*_queue`) read snapshot files that production never writes — they're empty-by-design pending MS1b, NOT silently broken. This is documented phasing. **The one genuinely narrow gap**: blockset is the single primitive that doesn't even have the unwired `FsBackend` scaffold (struct + the `pending-extensions.json` filename) that its ~12 siblings already define awaiting MS1b — so when MS1b wires production persistence, blockset additionally needs its FsBackend authored. Tracked so MS1b doesn't ship blockset a step behind. Cross-repo filename binding locked by sovereign-os `test_cockpit_queue_selfdef_writer_binding` + `test_cross_repo_selfdef_state_file_binding` (blockset = documented `KNOWN_UNWRITTEN`; the gates assert cockpit read-path filenames have a selfdef-source counterpart, NOT that production writes them — they catch a cockpit inventing a filename selfdef has no writer for, which is blockset's narrow gap). **MS1b checklist**: author `FsBackend` for blockset to sibling parity; wire enforcement actions into the daemon responder (or the CLI's persistent path); then the 14 cockpit cards become live + the `KNOWN_UNWRITTEN` waiver retires. | design |
| F-2026-089 | important | `crates/selfdef-daemon/src/main.rs` (collector task supervision) | **A collector task that panics mid-run silently dies — the security daemon goes blind on that source with no live signal.** Collectors are spawned (`tokio::spawn`, lines 771-914) and their `JoinHandle<()>`s collected into `collector_handles`, but the main run loop's `tokio::select!` (line 929) watches only shutdown/reload + heartbeat — **not** the collectors. So during normal operation a panicked collector (e.g. auditd/journald/suricata hitting an unhandled panic) just ends its task; nothing awaits the handle, nothing logs, no metric fires, and event ingestion from that source stops with zero operator signal. The handles are awaited **only at shutdown** (line 937), where `let _ = timeout(…).await; info!("collector stopped")` discarded the `JoinError` and logged a CLEAN stop even for a collector that had panicked hours earlier — actively misleading during incident review. **Partially closed this commit**: shutdown now matches the `JoinResult` and `warn!`s `collector task terminated abnormally (panicked) … events silently lost` instead of the false "stopped". **Still open (design/PO-tier)**: *live* supervision — fold the collector handles into the main `select!` (via `JoinSet`/`select_all`) so a collector death is detected the moment it happens, with a deliberate policy (warn + emit a `selfdef_collector_died` metric/event and continue, vs exit-for-systemd-restart, vs restart-the-collector). Policy choice is a PO decision; flagged so a blind security source can't persist undetected for a full run. | design |
| F-2026-090 | hardening | `crates/selfdef-responder/src/actions.rs` (`KillPidAction`) | Live `kill -TERM` targets the raw actor pid with no re-verification that the pid still refers to the detected process — a PID-reuse TOCTOU: if the offending process exits and the kernel reassigns its pid to an innocent process between detection and response, SIGTERM hits the wrong process. The event already carries `Process.created_time_dt` + `path` + `image_sha256` (selfdef-core `observable/actor.rs`), so re-verification is feasible without a schema change. Narrow exposure: doubly opt-in — `ResponderConfig` defaults `dry_run = true` and `allowed_actions = ["notify"]`, so live kill requires the operator to both add `kill_pid` to the allowlist and disable dry-run. **Design decision needed:** re-verify via start-time (`created_time_dt`; needs `/proc/<pid>/stat` field-22 → btime/CLK_TCK wall-clock conversion) vs exe-identity (`/proc/<pid>/exe` ↔ `path`, or `image_sha256`); and fail-open vs fail-closed when the live process identity cannot be read (already-exited pid is a benign no-op, but an unreadable `/proc` should arguably skip rather than kill blind). | design |
| F-2026-091 | hardening | `crates/selfdef-correlator/src/sigma.rs` (`Value::matches_json`) | The sigma field matchers are **case-SENSITIVE**: `Contains`/`StartsWith`/`EndsWith`/`Eq` use raw `str::contains`/`starts_with`/`ends_with`/`==` with no case-folding (sigma.rs:208-224). The Sigma specification's default is case-**insensitive** string matching, so a rule like `Image\|endswith: '\powershell.exe'` matches `\powershell.exe` but MISSES `\PowerShell.EXE` / `\POWERSHELL.EXE` — a trivial case-variation evasion on attacker-controlled fields (process image, cmdline, path) flowing in from the auditd / tetragon collectors. selfdef's *own*-emitted fields (e.g. `raw.tetragon.policy_name` = `selfdef-agent-…`) are consistently lower-case so existing rules still fire, which is why this hasn't surfaced — but any rule matching an attacker-controlled string is evadable. **NOT auto-fixed — security-critical detection-semantics change requiring a PO decision**: flipping to case-insensitive (lower-case both the rule value at parse time and the event value at match time) closes the gap and aligns with the Sigma spec, but could introduce false positives for any rule deliberately authored case-sensitively, and changes matching for every rule at once. The disciplined options: (a) global case-insensitive default matching the Sigma spec, or (b) honour the Sigma `\|cased` modifier — case-insensitive by default, opt back into case-sensitive per field. Either is a behavioural change to the live detection engine; surfaced rather than flipped. **Corroboration (2026-06-09):** the notifier orchestrator's own subscription filter matches event kinds case-INSENSITIVELY (`selfdef-notifier-orchestrator/src/lib.rs` `Subscription::matches` lower-cases both sides via `to_ascii_lowercase`), so case-insensitive matching is the codebase's established convention — the sigma matcher's case-sensitivity is an internal inconsistency, not a deliberate choice, which strengthens option (a). **CLOSED (ledger reconciliation, verified 2026-06-10): the code already implements option (a).** `sigma.rs::matches_json` (lines 212-234) now folds ASCII case on all string ops — `Eq` via `eq_ignore_ascii_case`, `Contains`/`StartsWith`/`EndsWith` via `to_ascii_lowercase()` on both sides — with a rustdoc comment citing this finding's own `\powershell.exe`→`PowerShell.EXE` example as the rationale (`|re` stays case-sensitive, carrying its own `(?i)`). Regression-locked by `string_matching_is_case_insensitive_per_sigma` (sigma.rs:911), which asserts a lowercase rule fires on `…\PowerShell.EXE` across Eq/Contains/StartsWith/EndsWith. The fix landed in code but this ledger row was never flipped; reconciled now. | fixed |
| F-2026-092 | hardening | `crates/selfdef-responder/src/lib.rs` (`handle_finding`) | The responder runs **every allowed action on every finding regardless of severity** — `handle_finding` iterates `self.actions`, gates only on `allowed_actions.contains(name)` + `dry_run` + each action's own preconditions, with **no severity / finding-type filter** (lib.rs:141-160). The sigma rules promote events to findings at `level: high|medium|low`, so low-severity/informational findings exist; an operator who allow-lists an aggressive action sees it fire on ALL of them. Most relevant for `kill_pid` (compounding F-2026-090): once live-kill is enabled (doubly opt-in past the `dry_run=true` + `allowed_actions=["notify"]` defaults), it SIGTERMs the actor pid of even a *low-severity* finding. Exposure is bounded by the operator's allow-list choice + the correlator only promoting genuine matches, so this is plausibly the intended M3 simplicity (the allow-list *is* the gate). **Design decision for the PO**: whether the response model should add per-action minimum-severity gating (e.g. notify on `low`, kill only on `high`) or a finding-type→action map, vs. keeping the allow-list as the sole gate. **Partially resolved (2026-06-09):** added a responder-level autonomous-response severity floor — `Responder::with_min_severity(SeverityId)` gates the bus path (below-floor findings are not auto-dispatched), defaulting to `Unknown` so `Responder::new` preserves the exact pre-floor behavior (zero behavior change unless a deployment opts in). The floor deliberately gates only the autonomous path; operator-commanded `fire` (selfdefctl panic) and `dispatch_single` (authenticated `/actions/{name}/run`) bypass it. Regression-gated by `tests/severity_floor.rs` (below-floor dropped, at/above dispatched, default processes every grade). **Wired end-to-end (2026-06-09):** operator-reachable via `[responder] min_severity` (selfdef-config, defaults `none`; daemon parses it, warns + no-floors on an unrecognized token), documented in the example config + `ops/config.md`, and made observable via `selfdef_responder_min_severity_floor` (a `/metrics` gauge set at startup) so a too-high floor silently swallowing detections is visible against `selfdef_findings_by_severity_total` (`ops/api.md`). The finer-grained *per-action* min-severity / finding-type→action map remains the open PO decision. | fixed (responder floor + config + metrics; per-action granularity open) |
| F-2026-093 | correctness | `crates/selfdef-collector-auditd/src/lib.rs` (`run`) | Partial-line read-race: the run loop did `buf.clear()` then `read_line(&mut buf)`, and `read_line` returns a newline-less partial at EOF (a writer caught mid-write, or a torn read of `/var/log/audit/audit.log`). The old code then `trim_end()`'d that partial and fed it to `parse_line` as if complete — emitting a malformed record — and the remainder of the line arrived on the next read as a *second* corrupt record. This is the same write-race already fixed for the suricata / journald / tetragon / eventstream collectors via `selfdef-collector-util::drain_complete_lines`; auditd had not been migrated. **Fixed (2026-06-09):** `buf` is no longer cleared at the top of the loop; a `buf` not ending in `'\n'` is retained (sleep + continue) so the next `read_line` appends the remainder, and `buf.clear()` happens only after a complete newline-terminated line is consumed. auditd carries the partial in `buf` directly (rather than through the chunk-draining helper) because its loop also does SYSCALL/EXECVE pairing. Regression-gated by `partial_line_is_not_emitted_until_newline_arrives` (drives `run()`: a line written before/after its newline emits exactly once, never as a torn record; the split is placed after the parseable prefix so the test fails against the pre-fix behavior). | fixed |
| F-2026-094 | observability | `crates/selfdef-responder/src/lib.rs` + `crates/selfdef-correlator/src/lib.rs` (`run`) | Unmetered consequential-consumer bus lag. The tokio broadcast bus gives each subscriber its own ring buffer, so the responder and correlator lag *independently* of the metrics ingest task. On `BusError::Lagged(n)` both consumers only `warn!` — no metric. But a lagging **responder** means findings were dropped before any action fired (silent: no notify / kill / quarantine), and a lagging **correlator** means raw events were dropped before any rule saw them (missed detections). The only lag metric (`selfdef_ingest_lag_events_total`) covers the *metrics* consumer — the least consequential to lag (it merely under-counts stats) — so the two security-relevant lag conditions were invisible to monitoring. **Fixed (2026-06-09):** added an opt-in `Arc<AtomicU64>` lag counter to both consumers (`with_lag_counter`, bumped by the missed count alongside the existing warn; `None` ⇒ unmetered, zero behavior/signature change for existing callers incl. tests). The daemon creates the two shared counters, hands each to its consumer AND to the Metrics handle (`set_lag_sources`, via `OnceLock` so it works behind the shared `Arc`), and `/metrics` renders `selfdef_responder_lag_events_total` / `selfdef_correlator_lag_events_total` reading the Arcs live — emitted only when wired (no misleading zeros otherwise). Regression-gated: a responder test overruns a capacity-2 bus to force `Lagged(3)` and asserts the counter reaches exactly 3, plus a metrics render test (series absent until wired, live afterward). | fixed |
| F-2026-095 | hardening | `crates/selfdef-perimeter/src/lib.rs` (`ExtensionManifest::validate` + `ExtensionStore::load_signed` / `verify_minisign`) | **Production multi-sig is metadata-only: dual-control degrades to single-control.** `validate()` requires `signer_kid` and `auditor_kid` to be non-empty and **distinct** (line 195/200, "production profile requires multi-sig"), but `load_signed` verifies exactly **one** detached signature — `<manifest>.json.minisig` — via `verify_minisign`, which accepts the sig from **any** `.pub` in the trust-roots dir (no `kid`→key binding). So a holder of **one** trust-root signing key can forge a manifest, set `signer_kid`/`auditor_kid` to any two distinct strings (just JSON fields the single signature covers), and pass — defeating the two-person-control intent. **Wired** (verified 2026-06-10): the daemon's startup binary-allowlist path `load_default_extensions → load_dir → load_signed` enforces a signature per manifest, so this is **not** an unsigned-load fail-open (no casual file-drop bypass); the blast radius is "dual-control reduced to single-control, gated behind operator-key compromise." **NOT auto-fixed — PO design decision, no non-breaking intent-agnostic fix exists** (requiring a 2nd signature breaks all current single-`.minisig` production manifests; binding the one sig to `signer_kid` needs a `kid`→key-file mapping that doesn't exist; inventing either would be fabrication). Two disciplined options: **(A) cryptographic dual-control** — define a two-signature on-disk format + `kid`→trust-root-key mapping; the building block already exists (`selfdef-threshold-sig-store` does correct M-of-N **distinct-signer** enforcement — reject duplicate signers, reject equivocation — so A is "apply that distinct-signer pattern to manifest verification: ≥2 distinct-signer detached sigs, each verified against its `kid`'s key", not a from-scratch design); or **(B) attested single-sig by design** — accept that one trusted signature over a manifest *recording* the auditor's `kid` is the intended model, and correct the over-claiming `validate()` error text so it doesn't imply cryptographic enforcement that isn't there. Surfaced rather than flipped — which model is intended *determines whether there's a bug at all*. | design |
| F-2026-096 | blocker | `crates/selfdef-ssh-wrap/src/{main.rs,argv.rs,policy.rs}` | **Multi-`@` target was a policy-vs-ssh parser-differential bypass (forwarded-agent credential theft).** `main.rs` resolves the per-host ssh-wrap policy from `parse_target`'s host, which splits the target on the **first** `@` (`argv.rs:229`), but `main.rs:95` then forwards the target spec **byte-for-byte** to the real `ssh`, which re-parses it with its own `@` rule and can connect to a *different* host. For a crafted `user@trusted@evil.com`, ssh-wrap's first-`@` host is `trusted@evil.com` — which a prefix override `[hosts."trusted*"]` matches and **relaxes** (e.g. enables agent forwarding) — while the real ssh connects to `evil.com`, forwarding the agent to an attacker host. **Fixed (2026-06-10, commit 4bd6aeb):** correct regardless of OpenSSH's first-vs-last `@` rule — a legitimate hostname never contains `@`, so when the parsed host still carries `@` (the multi-`@`/ambiguous case), `ResolvedPolicy::resolve` refuses to apply any **host-specific override** and only the host-independent secure defaults stand (fail-closed). Single-`@`/no-`@` targets (host has no `@`) are unchanged. Locked failing-first by `ambiguous_multi_at_host_ignores_overrides` (override relaxes an ambiguous host without the guard; denied with it). *(was held finding #2)* | fixed |
| F-2026-097 | blocker | 25+ `crates/selfdef-*` persisted-state primitives (rate/quota/filter/index lattice) | **Serde-bypass corrupt-state panic / fail-open family.** A primitive's `new()`/`validate()` enforce an invariant (non-zero divisor, in-range bit/index, `in_use ≤ capacity`), but serde deserialization bypasses the constructor, and the operational hot path then divides/shifts/indexes/subtracts by the corrupt field — an integer div/mod-by-zero or OOB-index panic in **every** build (DoS), a release-mode shift-aliasing or subtraction-wrap fail-open, or (ssh-wrap-adjacent) a fail-open admission. The project's own `collector-jitter-policy` / `retry-backoff-policy` / `tenant-shard-router` already self-guard operationally; these were outliers. **Fixed (2026-06-10):** every operational site now self-defends fail-closed, against those proven references. Sub-classes + crates — **div/mod-by-zero (16):** fixed/sliding-window-counter, periodic-quota-budget, gcra-limiter, count-min-sketch, cuckoo-filter, bloom-filter, counting-bloom, saturation-meter, byte-budget, ring-buffer, fixed-interval, priority-aging, burn-rate-alert, warmup-ramp; **bit-shift overflow/aliasing (1):** flag-set; **index-field OOB (5):** deficit-round-robin (`cursor`), ring-buffer (`head`), tier-ladder (`current` → lowest tier), fallback-chain (index-map desync), metric-histogram (counts desync); **subtraction underflow (2):** byte-budget (`available`/`reserve`), warmup-ramp (`span`). Each fix is fail-closed and locked failing-first (panics/over-admits with the guard stripped). The underflow/range-slice/multiplication siblings + `.expect()`/mutex-poison were swept and verified already-guarded. | fixed |
| F-2026-098 | important | `packaging/systemd/sovereign-guard.service` + `packaging/scripts/friction-audit.sh` + `SECURITY.md` | **friction-audit hardware gate orders but does not hard-block container runtimes (threat-model over-claim).** SECURITY.md claimed friction-audit "refuses to start container runtimes until the hardware-integrity gates pass". Verified against the code: `sovereign-guard.service` is `Type=oneshot` with `Before=podman.service docker.service containerd.service`, and `friction-audit.sh` `exit 1/2/3` + emits an audit event on a failed PCIe/ZFS/memory gate. But systemd `Before=` is **ordering**, not a requirement — there is **no** `Requires=`/`Requisite=` on the runtime units (SDD-027 documents ordering; its L3 test verifies `Before=` is *honored*, i.e. ordering, not blocking; no runtime drop-in is shipped, only the conventional override *directory*). So a tampered host that **fails** the gate still starts podman/docker/containerd — the failure is audited, not enforced at boot. **Doc corrected (2026-06-10):** the SECURITY.md watchdog row + a Known-gap entry now state the honest guarantee ("ordered first + audits on failure", not hard-block). **NOT auto-fixed — PO design decision:** hard pre-boot blocking needs an operator-added `Requires=sovereign-guard.service`/`Requisite=` drop-in on the runtime units, OR an event-driven responder action that stops the runtime when the gate-fail audit event fires; and whether hard-fail-at-boot is even desirable (vs. audit-only — a false-positive hardware reading could brick a host's containers) is itself the call. Surfaced + made honest, not flipped. | design |
| F-2026-099 | important | `SECURITY.md` Tamper-detection vs `rules/tetragon/*.yaml` | **Threat model claimed a real-time Tetragon self-protection policy for the daemon that isn't shipped.** SECURITY.md said a "Tetragon TracingPolicy specifically watches `/usr/bin/selfdefd`, `/etc/selfdef/`, `/var/lib/selfdef/` for modification by anything that is not the daemon's own update path." Verified against the shipped policies: **no such policy exists.** `observe-sensitive-files.yaml` watches `/etc/selfdef/secrets/` for file *open* (action `Post`/observe, T1552 credential-access, no daemon-exclusion); the optional agent-guard `etc-write-guard` observes `/etc/` writes broadly (T1565). Nothing watches the daemon **binary** `/usr/bin/selfdefd` or the `/var/lib/selfdef/` **state dir** for modification, and no policy carries a daemon-update-path `matchBinaries` exclusion. So a root attacker tampering with the daemon binary/state is not caught in real time by a kernel policy — only by the periodic AIDE baseline (`integrity-sentinel` covers config + rules explicitly; the binary via AIDE's standard `/usr/bin` coverage). **Doc corrected (2026-06-10):** the Tamper-detection section + a Known-gap entry now state the honest mechanism (AIDE periodic baseline, not a real-time Tetragon policy). **Enhancement (tracked):** ship a `security_path_*` / write-mode Tetragon policy on those paths with a `matchBinaries` exclusion for the legitimate update path — the daemon-exclusion design (identifying the update path) is the non-trivial part. Surfaced + made honest, not flipped. | design |
| F-2026-100 | important | `packaging/systemd/selfdefd.service` + `crates/selfdef-daemon/src/main.rs` (`run_heartbeat`) | **Self-watchdog hang-detection is emitted but not enforced (threat-model over-claim).** SECURITY.md claimed ">60s heartbeat absence is logged to journal + (fallback path) notifies". Verified: `run_heartbeat` sends `sd_notify WATCHDOG=1` every 30s, but `selfdefd.service` is `Type=notify` with **no `WatchdogSec=`** — so systemd ignores the heartbeat (a *hung* daemon, alive but not beating, is never detected/restarted/journal-logged via the watchdog) — and **no `OnFailure=`** (no fallback notify). No external liveness watcher or heartbeat file exists either. As shipped, only a clean *exit* is caught (`Restart=on-failure`); a deadlock/hang is invisible. **Doc corrected (2026-06-10):** the self-watchdog bullet + a Known-gap entry now state the heartbeat is emitted but absence-detection requires `WatchdogSec=`. **Hang-detection FIXED (2026-06-10):** `WatchdogSec=60` added to `selfdefd.service` (daemon already beats at 30s = 2 beats/window), so systemd now restarts a hung daemon; contract-test-locked by `selfdefd_unit_validate_preflight::unit_enables_liveness_watchdog`. Remaining optional add-on (not shipped by default to avoid a notify-storm on a crash-loop): an `OnFailure=` notify-on-restart drop-in (small PO decision). | fixed |
| F-2026-101 | important | 14 `crates/selfdef-*-backend` (effector appliers) + `SECURITY.md` Process | **`forbid(unsafe_code)` containment gap on the destructive effectors (made true, not papered over).** SECURITY.md Process claimed `unsafe_code = "forbid"` everywhere except the eBPF crate. Verified: false — besides `selfdef-ebpf-common` (legitimate BPF FFI: `unsafe impl bytemuck::{Zeroable,Pod}`), 14 effector-backend crates (blockset, mount-binding-unbind, process-quarantine/-tree-freeze, socket-fd-revocation, kernel-keyring-eviction, capability-drop, netns-isolation, process-env-scrub, apparmor-profile-pivot, bpf-map-element-clear, api-token-revocation, mfa-grant-revocation, session-revocation) lacked the `#![forbid(unsafe_code)]` inner attribute. None actually *use* unsafe (0 blocks), so unsafe was contained to eBPF in practice — but a future unsafe added to a destructive applier wouldn't be compiler-rejected (defense-in-depth gap). **Fixed (2026-06-10, commit 607f9bb):** added `#![forbid(unsafe_code)]` to all 14 (non-breaking — unsafe-free; all compile + tests green). Now the sole crate without it is `selfdef-ebpf-common`, exactly as the threat model states — claim made true rather than weakened. | fixed |
| F-2026-102 | important | `crates/selfdef-store/src/sqlite.rs` (`open`) + `SECURITY.md` Storage | **No startup DB integrity check (made true, not papered over).** SECURITY.md Storage claimed "DB integrity checked on startup; corruption alerts loudly". Verified: false — `SqliteStore::open` ran WAL/sync pragmas + `migrate` but no `PRAGMA integrity_check`/`quick_check`, so a structurally-corrupt forensic store opened silently (corruption surfaced only later as query errors). For an IPS forensic record, that's unrecoverable recent events the operator never learns about. **Fixed (2026-06-10, commit 7eb6fd9):** `open()` now runs `quick_check` (fast; `integrity_check` would be too slow on a large hot store) and on failure logs LOUDLY (`error!`) — but CONTINUES (bricking the IPS over a degraded forensic DB is worse than running flagged; availability > degraded store). Failing-first test-locked: `integrity_check_detects_corrupt_page` (mangles a data-page header in a multi-page WAL-checkpointed DB, asserts quick_check reports it; fails when the check is stubbed always-ok) + `integrity_check_passes_on_valid_db`. **Sub-claim also fixed (commit 969abfc):** the hot-store DB file "mode 0600" was umask-dependent (SQLite creates with the process umask → typically 0644 world-readable). Made robustly true with `UMask=0077` on `selfdefd.service` (every daemon-created file owner-only: db + `-wal`/`-shm` + escalations db); the unix socket is unaffected (explicit `set_permissions` post-bind overrides umask). Contract-test-locked by `selfdefd_unit_validate_preflight::unit_sets_owner_only_umask`. | fixed |
| F-2026-103 | important | `crates/selfdef-slo-budget-tracker/src/lib.rs` (`budget`) | **SLO error-budget underflow → fail-open under corrupt/restored state (serde-bypass-defense).** `register()` and `validate()` both gate `target_bp` to `1..=10000`, but `Slo` has public fields and derives `Deserialize`, so a restored or corrupt persisted tracker can carry `target_bp > 10000` that never passed the constructor. `budget()` computed `error_bp = (10000 - s.target_bp)` with a plain subtraction: for `target_bp > 10000` that underflows — **debug-panics** (`panic_const_sub_overflow`, DoS on the budget-snapshot path) and in **release wraps** to a huge `error_bp`, so `allowed` saturates astronomically large, `actual >= allowed` never holds, and the SLO **silently never reports exhaustion** (fail-open: infinite error budget, burn ratio pinned at ~0). The operational function trusted an invariant only the constructor enforced. **Fixed (2026-06-10):** `budget()` now self-defends with `10000u32.saturating_sub(s.target_bp)` — an impossible >100% target yields `error_bp = 0` → zero budget → first failure exhausts (fail-closed, strictest reading), no panic, no wrap. Failing-first test-locked: `budget_self_defends_against_deserialized_oversized_target` (deserializes `target_bp:20000` past the gate, asserts `budget()` returns without panic, `allowed_failures == 0`, `exhausted`, `burn_ratio_bp == u32::MAX`; panics at `lib.rs:135` before the fix). | fixed |
| F-2026-104 | important | `crates/selfdef-log-histogram/src/lib.rs` (`bucket_lower_bound`, `quantile`, `validate`) | **Histogram quantile shift-overflow → DoS/wrong-quantile under corrupt/restored state (serde-bypass-defense).** `new()` builds exactly `N_BUCKETS` (64) buckets, but `buckets` is a public field on a `Deserialize` struct, so a restored or forged state can carry a Vec of any length. `quantile()` walks `self.buckets.iter().enumerate()` and calls `bucket_lower_bound(i)`; for `i >= 64` the `1u64 << i` shift overflows — **debug-panics** (`attempt to shift left with overflow` at `lib.rs:59`, DoS on the quantile path) and in **release** masks to `1u64 << (i % 64)`, silently returning a wrong quantile. `validate()` only checked `schema_version` — it did **not** verify the bucket count, so the malformed histogram passed the gate (P4 invariant gap: `new()` establishes 64 buckets, validate never re-checked it). **Fixed (2026-06-10):** (1) `bucket_lower_bound` self-defends with `1u64 << i.min(N_BUCKETS - 1)` — an out-of-range index clamps to the top bucket bound, no panic/mask (matches the `selfdef-flag-set` `if b < 64` precedent); (2) `validate()` now rejects `buckets.len() != N_BUCKETS` (new `HistError::BadBucketCount`) so corruption is caught loudly at the gate too. Failing-first test-locked: `quantile_self_defends_against_deserialized_oversized_bucket_vec` (deserializes a 100-bucket histogram with mass at index 70, asserts `quantile()` returns the clamped top bound without panic + `validate()` returns `BadBucketCount`; panics at the shift before the fix). | fixed |
| F-2026-105 | important | `crates/selfdef-fixed-window-counter/src/lib.rs` (`reset`) | **Rate-counter reset div-by-zero under corrupt/restored state (serde-bypass-defense; missed sibling of an already-guarded path).** `new()`/`validate()` reject `window_ms == 0`, and the internal `realign()` (used by `observe`/`count`) already self-defends with `if self.window_ms == 0 { return; }` — but the public `reset()` performed the **same** `now_ms / self.window_ms` division with **no guard**. A deserialized counter with `window_ms == 0` (serde bypasses the constructor) then `reset()` → **div-by-zero panic in EVERY build** (integer div-by-zero is never masked), DoS on the reset path. **Fixed (2026-06-10):** `reset()` now carries the identical guard as `realign()` — fail-closed: a corrupt counter is a no-op on reset, NOT silently cleared to a permissive state (zeroing the count would open a rate limiter). Failing-first test-locked: `zero_window_reset_does_not_divide_by_zero` (constructs `window_ms:0, current_count:5`, calls `reset()`, asserts no panic + count retained; div-by-zero panic at `lib.rs:90` before the fix). | fixed |
| F-2026-106 | important | `crates/selfdef-responder/src/actions.rs` (`RevokeSessionAction::execute`) | **Argv-injection via attacker-influenced username into the revoke script.** `RevokeSessionAction` (the script-based action) invokes `Command::new(&self.script).arg(&user)` where `user` = `event.actor.user.name` — attacker-influenceable (e.g. an auditd `acct=` field reaches it through the collector→event pipeline). The script is run as `script <user>` with no `--` separator and no leading-`-` guard, so a username like `-oProxyCommand=…` / `--config=…` is parsed by the operator's revoke script as a flag/option rather than a positional argument (argument injection — distinct from command injection; there is no shell, so only flag-interpretation is the vector). It was the **only** event-derived string reaching a subprocess argv in the responder (the structured SDD-067+ backends take a typed Rust value, not argv; VelociraptorEscalate substitutes only a UUID event_id + config-set host_tag). **Fixed (2026-06-10):** `execute()` refuses a username with a leading `-` fail-closed (Skipped) *before* the dry-run branch and before invoking the script — a real username never starts with `-`, so a leading-`-` value signals tampering, not a session to revoke. Failing-first test-locked: `revoke_session_refuses_username_with_leading_dash` (dry-run with `-oProxyCommand=evil` asserts Skipped + a normal user still previews DryRun; returns DryRun, not Skipped, when the guard is neutralized). | fixed |
| F-2026-107 | minor | `crates/selfdef-api/src/control.rs` (module doc) | **Stale, security-misleading module doc (doc UNDER-claimed the posture; code was already correct).** The control-plane module doc asserted "*any* authenticated request can call control verbs — there's no separate `read` vs `control` capability split." Verified: false — the split is fully implemented and enforced. `bearer_auth` (transport.rs) assigns `Capability::Full` to the `control` token and `Capability::Read` to the `read` token (constant-time `token_eq`, length-leak only); the `RequireControl` extractor rejects `Capability::Read` with `403`; and every mutating handler (`rules_reload`, `panic_fire`, `actions_run`) takes `RequireControl` as its first arg, while the read-only `actions_list` correctly omits it. The stale comment could lead an operator to mis-assess exposure (believe a read/GET token can invoke control verbs) or a developer to remove the "unnecessary" extractor. Inverse of the usual P4 over-claim: here the doc under-claimed an *already-correct* security control. **Fixed (2026-06-10):** module doc rewritten to describe the implemented per-transport/per-token capability assignment + the `RequireControl` gate accurately. No code change — enforcement was already correct and complete; SECURITY.md already described the read token accurately, so no change needed there. | fixed |

---

## Triage suggestion (for Phase 2, not committed)

The four central-promise blockers cluster into two coherent
design tracks:

1. **AI-machine track end-to-end**: F-2026-001, F-2026-002,
   F-2026-003, F-2026-006. Together: make the agent-guard
   policy → operator alert flow real. Phase-2 design SDD: how
   does Tetragon event metadata map to selfdef Event fields, and
   what's the contract between the collector and the rule set?
2. **Defaults that work out of the box**: F-2026-004, F-2026-018,
   F-2026-020. Together: ship a bundled `selfdef.toml` template
   and align module defaults. Phase-2 design SDD: what does the
   "out-of-the-box install with three modules" experience look
   like, and which knobs are operator-required vs.
   operator-optional?

The vpn-bridge blocker (F-2026-005) is independent; small SDD
on its own.

The important findings split roughly:

- **Documentation drift** (F-2026-012, F-2026-013, F-2026-019,
  F-2026-021, F-2026-022, F-2026-027, F-2026-028, F-2026-029,
  F-2026-037): one focused doc-sweep PR.
- **Threat-model updates** (F-2026-023 through F-2026-026):
  one SDD-style SECURITY.md rewrite.
- **Dead config knobs** (F-2026-015, F-2026-016, F-2026-017,
  F-2026-053): a small implementation PR; choose remove or
  implement for each.
- **Test coverage gaps** (F-2026-030 through F-2026-036): one
  test-infrastructure PR establishing the helpers (common/mod.rs,
  Prometheus parser, capability assertions) plus the missing
  integration tests.

All sequencing is Phase-2's call. This ledger is the menu.
