# selfdef aslr-baseline — full.
# kernel.randomize_va_space values:
#   0 = no randomization (INSECURE — exploits become deterministic)
#   1 = partial (stack, VDSO, shared libs; NOT heap/brk)
#   2 = full (stack, heap, mmap, VDSO, brk) — the secure target
kernel.randomize_va_space = 2
