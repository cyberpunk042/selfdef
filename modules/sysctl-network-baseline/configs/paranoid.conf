# selfdef sysctl-network-baseline — paranoid profile.
# Baseline + disable IPv6 entirely + silent host (no ICMP
# echo reply). Use only when operator has verified no IPv6
# dependency and accepts that ping/traceroute will not work.

# === Baseline (mirrored) ===
net.ipv4.conf.all.accept_source_route = 0
net.ipv4.conf.default.accept_source_route = 0

net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.default.accept_redirects = 0

net.ipv4.conf.all.secure_redirects = 0
net.ipv4.conf.default.secure_redirects = 0

net.ipv4.conf.all.send_redirects = 0
net.ipv4.conf.default.send_redirects = 0

net.ipv4.conf.all.rp_filter = 1
net.ipv4.conf.default.rp_filter = 1

net.ipv4.conf.all.log_martians = 1
net.ipv4.conf.default.log_martians = 1

net.ipv4.icmp_echo_ignore_broadcasts = 1
net.ipv4.icmp_ignore_bogus_error_responses = 1
net.ipv4.tcp_syncookies = 1

# === Paranoid additions ===

# Silent host — refuse to reply to ICMP echo at all.
# Breaks ping AND traceroute. Operator accepts this trade.
net.ipv4.icmp_echo_ignore_all = 1

# Disable IPv6 globally.
net.ipv6.conf.all.disable_ipv6 = 1
net.ipv6.conf.default.disable_ipv6 = 1
net.ipv6.conf.lo.disable_ipv6 = 1
