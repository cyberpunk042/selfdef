# selfdef kernel-yama-baseline — relaxed profile.
# ptrace_scope = 1: a process can only ptrace its descendants
# (parent → child or further). Matches Ubuntu's default since
# 10.10. Breaks zero workflows on a properly-configured host.
kernel.yama.ptrace_scope = 1
