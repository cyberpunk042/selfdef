# selfdef kernel-yama-baseline — strict profile.
# ptrace_scope = 2: only processes with CAP_SYS_PTRACE may
# ptrace any process. Standard non-root users cannot ptrace
# their own processes — gdb attach by operator requires
# sudo OR file-capability assignment to gdb.
kernel.yama.ptrace_scope = 2
