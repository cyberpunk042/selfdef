# selfdef kernel-sysrq-restrict — full.
# All Magic SysRq commands enabled. Distro default on some
# desktop distros. Reserved for development hosts where the
# operator regularly needs sync / dump / OOM-trigger.
kernel.sysrq = 1
