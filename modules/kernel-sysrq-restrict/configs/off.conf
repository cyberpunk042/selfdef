# selfdef kernel-sysrq-restrict — off.
# Disables Magic SysRq entirely. Anyone with physical access
# (or serial console) can no longer trigger kernel-level
# reboot / kill-all / sync / dump primitives.
kernel.sysrq = 0
