# Managed by the selfdef `observability` module. The module's
# apply.sh renders this file with the operator's `scrape_targets`
# spliced into the `static_configs.targets` list.

scrape_configs:
  - job_name: "selfdef-tetragon"
    scrape_interval: 15s
    metrics_path: "/metrics"
    static_configs:
      - targets:
          # __SELFDEF_SCRAPE_TARGETS__ — rewritten by apply.sh
          - "localhost:2112"
        labels:
          source: "tetragon"
          managed_by: "selfdef"
