# selfdef file-protections-baseline — strict profile.
# Same as baseline (kernel has no higher than 2 for fifos/
# regular; 1 for hardlinks/symlinks). Reserved for future
# kernel options.
fs.protected_hardlinks = 1
fs.protected_symlinks = 1
fs.protected_fifos = 2
fs.protected_regular = 2
