# selfdef umask-baseline — group profile. /etc/login.defs.d
# drop-in for non-interactive PAM-spawned shells (cron, ssh
# pre-shell, etc.).
#
# login.defs UMASK is consulted by useradd + login + cron when
# spawning shells; profile.d only applies to interactive sources.

UMASK 027
