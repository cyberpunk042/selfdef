# selfdef pam-history — standard.
# Block reusing any of the last 5 passwords on a passwd change.
# enforce_for_root=no — recovery-friendly (root can re-use as
# needed under emergency conditions).
remember = 5
retry = 3
enforce_for_root = no
