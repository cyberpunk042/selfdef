# selfdef unprivileged-userns-baseline — allow.
# Documents the open-by-default posture; required for rootless
# containers + bubblewrap-based sandboxing (Flatpak, Steam,
# Firefox renderer, Chromium renderer).
kernel.unprivileged_userns_clone = 1
