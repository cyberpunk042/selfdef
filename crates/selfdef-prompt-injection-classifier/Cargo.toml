[package]
name = "selfdef-prompt-injection-classifier"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS prompt-injection classifier. Pure pattern-matching authority that scores inbound text against an enumerated set of injection signals (instruction overrides, system-prompt extraction, role-hijack, exfiltration solicit, jailbreak chain). Emits a class, a confidence, and the matched signals. No LLM call — this is the substrate's first-line defense, deterministic and auditable."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
