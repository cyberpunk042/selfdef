[package]
name = "selfdef-clock-skew"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Clock-skew tracker — mean offset + min/max between two clocks."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
