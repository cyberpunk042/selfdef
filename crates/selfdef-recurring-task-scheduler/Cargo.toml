[package]
name = "selfdef-recurring-task-scheduler"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Periodic recurring-task scheduler with fixed-interval tick."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
