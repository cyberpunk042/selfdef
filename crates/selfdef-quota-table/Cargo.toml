[package]
name = "selfdef-quota-table"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Per-key quota table — consume with auto-reset per period."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
