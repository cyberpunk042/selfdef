[package]
name = "selfdef-step-curve"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Piecewise step function — lookup by x to highest x_i <= x."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
