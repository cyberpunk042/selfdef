[package]
name = "selfdef-bus-subscriber-registry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side event-bus subscriber registry — 9 canonical subscribers (AuditLog / Quarantine / Notifier / EvidenceLedger / HistorySink / PolicyBus / TrustScore / ProfileAuthority / DashboardManifest) each declaring delivery class (Broadcast / Queue / Signaled). Refuses bus start if any subscriber unwired or marked Quarantined."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
