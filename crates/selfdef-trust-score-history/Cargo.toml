[package]
name = "selfdef-trust-score-history"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side append-only per-subject trust-score history — every score change is recorded with (subject, old_score, new_score, delta, reason, at). The trust-score-engine appends here; the cockpit reads through a typed-mirror crate."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
