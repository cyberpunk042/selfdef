[package]
name = "selfdef-mount-binding-unbind-backend"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "SDD-071 MS1 — mount-binding unbind backend trait + in-memory test impl. Seventh IPS enforcement primitive extending hexet → septet at the filesystem-binding axis (pairs with SDD-066 process-freeze + SDD-070 netns-isolation)."
license = "MIT OR Apache-2.0"

[dependencies]
async-trait = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }
serde_json = { workspace = true }
