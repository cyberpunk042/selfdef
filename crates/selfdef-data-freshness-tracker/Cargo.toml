[package]
name = "selfdef-data-freshness-tracker"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Per-key data freshness tracker with Fresh/Stale/Expired bands."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
