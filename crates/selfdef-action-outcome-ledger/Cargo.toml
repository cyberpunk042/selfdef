[package]
name = "selfdef-action-outcome-ledger"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Action outcome ledger with per-class counters and recent ring buffer."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
