[package]
name = "selfdef-host-fingerprint-attestation"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side host fingerprint attestation — operator attests to a specific host fingerprint (kernel + cpu + boot id + filesystem id) at install time. The daemon refuses any new boot where the live fingerprint diverges without a signed re-attestation."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
