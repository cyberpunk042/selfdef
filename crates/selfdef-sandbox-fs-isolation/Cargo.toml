[package]
name = "selfdef-sandbox-fs-isolation"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS sandbox-tier filesystem isolation. Each SandboxTier declares an allowed root-class set (None/JailDirOnly/WorkspaceOnly/UserHome/Host) and a write_mode (ReadOnly/ReadWrite). decide(tier, path_class, op) gates a read/write request; tier upgrades widen access but never grant Host writes from Tier<4."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
