[package]
name = "selfdef-token-estimator"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Cheap rough-token-count estimator (chars / divisor)."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
