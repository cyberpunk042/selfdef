[package]
name = "selfdef-counter-by-key"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Per-key counter with top-K and total queries."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
