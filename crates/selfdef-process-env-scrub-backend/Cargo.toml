[package]
name = "selfdef-process-env-scrub-backend"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "SDD-074 MS1 — process-env scrub backend trait + in-memory test impl. Tenth IPS enforcement primitive extending nonet → dectet at the in-memory secret-residency axis (pairs with SDD-068 token-revocation)."
license = "MIT OR Apache-2.0"

[dependencies]
async-trait = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }
serde_json = { workspace = true }
