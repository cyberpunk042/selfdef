[package]
name = "selfdef-llm-stream-cutoff-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS LLM streaming cutoff policy. As tokens stream in, the gate watches for (max_tokens reached / stop_sequence emitted / repetition trip / wall_seconds elapsed) and emits a typed CutoffReason. Prevents runaway generation from blowing past per-mode budgets."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
