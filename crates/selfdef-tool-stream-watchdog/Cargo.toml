[package]
name = "selfdef-tool-stream-watchdog"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS tool-stream watchdog. Watches a streaming tool output for SilenceTimeout (no bytes for N ms) and TotalTimeout (overall ms cap). tick(now, bytes_so_far) emits typed StreamVerdict to the dispatcher."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
