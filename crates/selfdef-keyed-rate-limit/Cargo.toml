[package]
name = "selfdef-keyed-rate-limit"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Per-key token-bucket rate-limit registry with default cap."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
