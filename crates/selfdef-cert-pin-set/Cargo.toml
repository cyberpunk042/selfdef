[package]
name = "selfdef-cert-pin-set"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Per-host certificate fingerprint pin store with TTL + grace overlap."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
