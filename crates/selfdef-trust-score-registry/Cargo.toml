[package]
name = "selfdef-trust-score-registry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Daemon-resident, persistent registry of per-tool trust scores — the live D-18 state the selfdef daemon projects into the selfdef-trust-score-mirror MS007 snapshot. Composes selfdef-trust-score-mirror (wire schema) + selfdef-trust-score-engine (canonical_delta) with daemon-recorded deltas + operator-signed manual overrides."

[lints]
workspace = true

[dependencies]
selfdef-trust-score-mirror = { path = "../selfdef-trust-score-mirror" }
selfdef-trust-score-engine = { path = "../selfdef-trust-score-engine" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
time = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
