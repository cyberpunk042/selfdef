[package]
name = "selfdef-substrate-thread-quota"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS substrate per-Profile concurrent-thread cap. spawn(label) returns a thread_id; finish(id) releases. Distinct from selfdef-substrate-{cpu,gpu,disk,network-egress,fd}-quota — this is the live-thread lane."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
