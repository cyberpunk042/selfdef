[package]
name = "selfdef-socket-fd-revocation-backend"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "SDD-073 MS1 — socket-fd revocation backend trait + in-memory test impl. Ninth IPS enforcement primitive extending octet → nonet at the per-connection severance axis (pairs with SDD-065 perimeter-block + SDD-070 netns-isolation)."
license = "MIT OR Apache-2.0"

[dependencies]
async-trait = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }
serde_json = { workspace = true }
