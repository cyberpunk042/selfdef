[package]
name = "selfdef-audit-rotation-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side audit log rotation policy — declares per-audit-class rotation trigger (Daily / Weekly / Monthly / BySize / OnEvent) + compression + retention floor. The audit-log-writer consults this before opening a new segment."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
