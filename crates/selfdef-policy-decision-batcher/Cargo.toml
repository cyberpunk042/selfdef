[package]
name = "selfdef-policy-decision-batcher"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side policy-decision batcher — accepts up to MAX_BATCH validated PolicyDecisions and produces one bundled emit. Hashes the batch payload (FNV-1a) so downstream subscribers can dedup or detect tampering."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
selfdef-policy-decision = { path = "../selfdef-policy-decision" }

[features]
default = []
