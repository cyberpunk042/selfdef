[package]
name = "selfdef-handler-registry"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Kind → handler routing registry with per-kind priority ordering."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
