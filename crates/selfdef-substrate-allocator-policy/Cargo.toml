[package]
name = "selfdef-substrate-allocator-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS substrate resource allocator. Per-ResourceClass (Heap/Stack/Mmap/Tmpfile) per-allocation max_bytes + total_bytes cap with a sliding cumulative counter. admit(class, bytes) returns Allow / DeniedPerAlloc / DeniedTotal."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
