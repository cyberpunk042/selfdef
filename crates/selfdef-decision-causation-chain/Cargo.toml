[package]
name = "selfdef-decision-causation-chain"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Decision causation chain: each decision links to its causes; walkable both directions."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
