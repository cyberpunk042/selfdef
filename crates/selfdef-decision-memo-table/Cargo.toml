[package]
name = "selfdef-decision-memo-table"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Cached decisions keyed on (input_hash, policy_version) with hit/miss telemetry."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
