[package]
name = "selfdef-evidence-tag"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side evidence-tag taxonomy — 6 canonical tags (Decision / Grant / Quarantine / Promotion / SelfTest / Anomaly) attached to every evidence-ledger entry so the operator can filter the ledger by class. Each tag declares retention class (eternal vs purgable) + cockpit visibility."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
