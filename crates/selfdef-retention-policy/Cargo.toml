[package]
name = "selfdef-retention-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side retention policy — per record-kind retention windows the daemon enforces against the on-disk audit/trace/decision stores. 8 canonical kinds (audit / decision / span / quarantine / evidence / trust-score / collector-sample / replay-buffer) with explicit retention semantics (Forever / Days(n) / EventCount(n) / SizeBytes(n))."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
