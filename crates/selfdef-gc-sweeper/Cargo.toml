[package]
name = "selfdef-gc-sweeper"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Age + size GC sweep over a string-keyed registry."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
