[package]
name = "selfdef-audit-digest"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side audit rollup digest — sliding-window counters over recent PolicyDecisions: per outcome (allow/deny/ask/sandbox), per side-effect class, per profile, per risk-class. The daemon publishes one digest per minute the cockpit dashboard subscribes to."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
selfdef-policy-decision = { path = "../selfdef-policy-decision" }

[features]
default = []
