[package]
name = "selfdef-resource-reservation"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS pre-action resource reservation. reserve(resource_id, units) holds against a per-resource capacity; commit consumes the reservation; abandon frees it. expire(now, max_age_ms) drops stale reservations. Pure ledger."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
