[package]
name = "selfdef-filesystem-boundary"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "MS037 filesystem boundary — 3-dir exchange (/ai-exchange/{inbox,outbox,artifacts}) + 6-step host import pipeline + 5-field patch schema + 6-check application predicates per E0371-E0376 + dump 3550-3593. Doctrine: 'Use explicit exchange directories' + 'VM writes proposals, not final state'."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
