[package]
name = "selfdef-mcp-tool-arg-constraint"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS per-tool per-argument value constraints. Declares Constraint enum (StrLenAtMost, StrEnum, IntRange, RequiredPresent, ForbidEmpty) and check(tool_id, args) returns Ok / Violations{list}. Pairs with selfdef-mcp-tool-validation (descriptor shape) — this checks the actual values at call time."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
