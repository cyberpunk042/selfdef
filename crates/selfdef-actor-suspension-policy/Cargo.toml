[package]
name = "selfdef-actor-suspension-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS per-actor suspension state with reason + auto-unsuspend at ts. suspend(actor, reason, unsuspend_at) records; classify(actor, now) returns Active/Suspended{reason,unsuspend_at}/Unknown. unsuspend(actor) clears immediately. Distinct from quarantine (per-event), this is the actor-wide moratorium lane."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
