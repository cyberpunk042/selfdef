[package]
name = "selfdef-decision-conflict-detector"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS detector for divergent decision outcomes on the same payload-hash within a sliding window. record(payload_hash, outcome, ts) updates the ledger; check(payload_hash, now) returns Consistent / Divergent{outcomes} / Stale. Distinct from selfdef-replay-divergence-detector (replay tape) — this is the live decision-outcome lane."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
