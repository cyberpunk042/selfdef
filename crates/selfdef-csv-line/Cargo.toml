[package]
name = "selfdef-csv-line"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Minimal CSV line splitter — RFC-4180-lite (configurable separator + quote)."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
