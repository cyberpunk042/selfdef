[package]
name = "selfdef-frequency-map"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Bounded key→count map with top-N retrieval and oldest-eviction."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
