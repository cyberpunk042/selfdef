[package]
name = "selfdef-event-log"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Append-only bounded event log with sequence ids + cursor consume."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
