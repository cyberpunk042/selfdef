[package]
name = "selfdef-session-revocation-backend"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "SDD-067 MS1 — session-revocation backend trait + in-memory test impl. The LoginctlBackend production adapter ships in MS1b under the `loginctl-backend` feature; this crate hosts the contract + InMemoryBackend for hermetic tests."
license = "MIT OR Apache-2.0"

[dependencies]
async-trait = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }
serde_json = { workspace = true }
