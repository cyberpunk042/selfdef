[package]
name = "selfdef-tenant-shard-router"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Deterministic tenant→shard routing with rebalance/sticky overrides."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
