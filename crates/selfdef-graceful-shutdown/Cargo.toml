[package]
name = "selfdef-graceful-shutdown"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Multi-stage graceful shutdown: stop-accept → drain → terminate with per-stage deadlines."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
