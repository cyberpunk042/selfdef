[package]
name = "selfdef-config-personalization-bounds"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS guard for operator-customizable settings. Each (Profile, knob_id) carries a Bound (IntRange{min,max} / EnumOf{allowed} / BoolEither / StrLenAtMost{max}). check(profile, knob_id, value) returns Ok / OutOfBound{bound, observed} / UnknownKnob / UnknownProfile. Prevents personalization from pushing knobs outside policy-defined bounds."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
