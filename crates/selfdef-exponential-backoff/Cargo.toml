[package]
name = "selfdef-exponential-backoff"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Exponential backoff calculator with deterministic jitter via FNV-1a-64."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
