[package]
name = "selfdef-secret-binding-store"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Per-actor named secret refs (vault path bindings; no raw secrets stored)."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
