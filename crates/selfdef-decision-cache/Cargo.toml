[package]
name = "selfdef-decision-cache"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side decision cache — sliding-window cache keyed by (subject, action, resource, profile) fingerprint, value = last Outcome. Lets the daemon short-circuit recent decisions without re-running the full policy gate, while still emitting a trace span tagged 'cache-hit'."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
selfdef-policy-decision = { path = "../selfdef-policy-decision" }

[features]
default = []
