[package]
name = "selfdef-substrate-cgroup-binding"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS Profile → Linux cgroup-name binding. classify(profile) returns Bound{cgroup_name} or Unconfigured. Pure mapping descriptor; the substrate uses the returned name to spawn the process tree into the right cgroup."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
