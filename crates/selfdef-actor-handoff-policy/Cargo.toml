[package]
name = "selfdef-actor-handoff-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS actor-handoff policy: delegated tasks where actor A grants actor B the right to act on a scope for a bounded window. grant(from, to, scope, expires_at); revoke; classify(from, to, scope, now) returns Active{expires_at}/Expired/Revoked/Unknown. Pure store."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
