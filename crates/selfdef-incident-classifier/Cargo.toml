[package]
name = "selfdef-incident-classifier"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side incident severity classifier — 5-level taxonomy (Info / Notice / Warn / Critical / Emergency) driving notifier routing and operator-attention surfacing. Each level has fixed dispatch targets (loki always; pagerduty/twilio at Critical+; phone/wall at Emergency)."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
