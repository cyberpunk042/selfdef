[package]
name = "selfdef-policy-cooldown-window"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS policy decision cooldown window. After a policy fires for (subject, action), it must not fire again within cooldown_seconds for the same pair. Prevents alert/notification fatigue when one subject repeatedly triggers a guard. Per-PolicyClass cooldown values."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
