[package]
name = "selfdef-action-trace-budget"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS per-action allowed trace-span count. start(action_id, max_spans) opens; admit(action_id) returns Accepted{used}/Exhausted{cap}/UnknownAction. finish(action_id) clears. Pure counter; distinct from selfdef-trace-sampling-policy (probabilistic sampling) — this is the absolute cap lane."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
