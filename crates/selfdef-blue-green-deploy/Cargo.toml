[package]
name = "selfdef-blue-green-deploy"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Blue/green deploy state — active/standby with controlled swap."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
