[package]
name = "selfdef-action-deadline-tracker"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Per-action deadline tracker with on-time/expiring/expired verdicts."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
