[package]
name = "selfdef-connection-pool"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Bounded connection-id pool with health cooldown + idle reaping."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
