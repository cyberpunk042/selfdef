[package]
name = "selfdef-route-table"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "String-key router — exact match first, longest-prefix fallback."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
