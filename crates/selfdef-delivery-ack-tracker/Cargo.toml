[package]
name = "selfdef-delivery-ack-tracker"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Per-message delivery ack/nack tracker with retry budget."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
