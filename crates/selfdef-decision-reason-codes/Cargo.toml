[package]
name = "selfdef-decision-reason-codes"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS canonical decision reason-code taxonomy. Each policy gate that denies/asks/sandboxes must reference one of the enumerated ReasonCodes (e.g., RC_TopSecretEgress, RC_QuotaExhausted, RC_NotWithinTimeWindow). Cockpit + audit + replay can all index by the same key so operator-facing messages stay consistent."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
