[package]
name = "selfdef-monotonic-clock"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Monotonic-clock guard — caches last ms, rejects regression."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
