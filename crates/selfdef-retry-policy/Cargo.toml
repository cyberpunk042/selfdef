[package]
name = "selfdef-retry-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS retry-policy authority. Per-FailureClass (Transient/Throttled/Auth/Permanent) max_attempts + base_delay_ms + multiplier + max_delay_ms + jitter. should_retry(class, attempt) returns Some(delay_ms) when retry permitted; Transient retries up to N times with exponential backoff; Permanent never retries."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
