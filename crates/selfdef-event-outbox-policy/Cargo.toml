[package]
name = "selfdef-event-outbox-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS transactional-outbox policy: append events with monotonic seq, then a flush() removes events ≤ confirmed_seq once an external sink confirms delivery. Pure ledger; durable-write pattern for at-least-once delivery."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
