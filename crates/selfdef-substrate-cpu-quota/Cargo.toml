[package]
name = "selfdef-substrate-cpu-quota"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS substrate CPU-time quota. Per-Profile per-minute CPU-second budget tracked via a sliding window. record_cpu_seconds(profile, seconds, now) updates; admit(profile, seconds, now) returns Allow/Deny based on projected window total."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
