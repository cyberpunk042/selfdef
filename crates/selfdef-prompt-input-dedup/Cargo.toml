[package]
name = "selfdef-prompt-input-dedup"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS sliding-window dedup of identical prompt-input hashes. observe(hash, ts) records; check(hash, now) returns Fresh / Duplicate{age_ms, original_ts}. The window width is configured per ledger. Avoids re-running deterministic prompts within a quiet period. Pure ledger; FNV-1a-style fingerprint expected from caller."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
