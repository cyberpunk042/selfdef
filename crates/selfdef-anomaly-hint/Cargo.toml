[package]
name = "selfdef-anomaly-hint"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side anomaly hint envelope — the daemon emits one hint per detected deviation (sudden risk-class rise, novel model_provider, off-hour activity, repeat-deny, capability-word drift, untrusted subject). Hints are non-blocking signals the operator dashboard surfaces; they never themselves quarantine."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
selfdef-incident-classifier = { path = "../selfdef-incident-classifier" }

[features]
default = []
