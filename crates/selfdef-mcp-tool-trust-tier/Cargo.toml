[package]
name = "selfdef-mcp-tool-trust-tier"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS per-MCP-tool trust tier (Sandbox/SemiTrusted/Trusted/Hardened) and per-Profile minimum-tier gate. register(tool_id, tier) records; classify(profile, tool_id) returns Allowed/BelowTier{needed,got}/UnknownTool/Unconfigured. Distinct from selfdef-tool-capability-policy (capability shape) — this is the trust-tier lane."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
