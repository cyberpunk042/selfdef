[package]
name = "selfdef-tool-version-pinning"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS tool-version pinning authority. Each pin is (tool_id, required_semver | exact_sha256). admit(tool_id, version, sha256) returns Allow / VersionMismatch / DigestMismatch / Unknown. Prevents a tool being silently swapped or upgraded under the engine."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
