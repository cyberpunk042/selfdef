[package]
name = "selfdef-deny-recurrence"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side deny-recurrence tracker — sliding window of recent denied decisions per (subject, action) pair. When threshold breached, emits a RepeatDeny anomaly hint. Companion to selfdef-anomaly-hint + selfdef-policy-decision."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
