[package]
name = "selfdef-delay-queue"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Time-ordered delay queue — schedule(id, ts, payload), poll(now)."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
