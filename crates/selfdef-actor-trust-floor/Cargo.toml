[package]
name = "selfdef-actor-trust-floor"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS per-Profile minimum trust-score floor an actor must meet to be permitted in that Profile. classify(profile, score 0..=1000) returns Allowed / BelowFloor{floor} / Unconfigured. Pure policy crate; companion to selfdef-trust-score-engine which produces the scores."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
