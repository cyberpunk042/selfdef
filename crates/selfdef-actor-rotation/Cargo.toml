[package]
name = "selfdef-actor-rotation"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side operator key rotation tracker — operator-signed key rotations recorded with (old_fp, new_fp, at, double-sig). Provides `is_current` + `is_retired` + `rotation_chain` so other crates can validate signatures against the right epoch."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
