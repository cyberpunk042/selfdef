[package]
name = "selfdef-trust-floor"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side per-action trust-floor — minimum trust score (0..100) a subject must hold to be allowed a given side-effect class. fs-write / network-egress / process / persistent each have a declared floor; subjects below floor are forced to Ask or Deny."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
selfdef-policy-decision = { path = "../selfdef-policy-decision" }

[features]
default = []
