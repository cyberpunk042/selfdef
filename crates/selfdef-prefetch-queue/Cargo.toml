[package]
name = "selfdef-prefetch-queue"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Dedup'd prefetch-hint queue with LRU eviction at capacity."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
