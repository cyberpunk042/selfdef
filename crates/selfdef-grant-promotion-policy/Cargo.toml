[package]
name = "selfdef-grant-promotion-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side grant promotion policy — authority over extending TTL or widening scope of an already-Active grant. Operator can extend by ≤ original TTL, or widen scope only via explicit re-application + double-operator signoff. Plain demotion (shorter TTL, narrower scope) is routine."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
