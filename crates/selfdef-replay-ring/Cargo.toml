[package]
name = "selfdef-replay-ring"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side bounded replay ring — fixed-capacity FIFO of recent envelope frames the daemon retains so an operator can replay the last N events through the pipeline for incident reconstruction. Capacity gate tied to selfdef-retention-policy::ReplayBuffer rule."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
