[package]
name = "selfdef-actor-group-membership"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS actor-group membership registry. add(group, actor); members_of(group); groups_of(actor); is_member(group, actor). Used by policies that gate on group rather than per-actor (e.g. 'oncall' / 'security-team'). Pure descriptor; group hierarchy lives in selfdef-policy-namespace-policy."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
