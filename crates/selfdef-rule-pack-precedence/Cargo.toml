[package]
name = "selfdef-rule-pack-precedence"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS rule-pack precedence authority. Each loaded rule pack declares a source class (Vendor/Operator/Substrate/Emergency) with fixed numeric precedence. resolve(packs) returns rules ordered so that higher-precedence packs' decisions are evaluated first, preventing vendor defaults from shadowing operator hand-overrides."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
