[package]
name = "selfdef-flex-profile"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "MS011 Z-3 flex-profile state — live delta over baseline YAMLs with revert history."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
