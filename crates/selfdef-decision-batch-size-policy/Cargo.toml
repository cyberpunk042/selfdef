[package]
name = "selfdef-decision-batch-size-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS decision-batch size policy. Bundles N pending decisions into a single LLM call when they share a DecisionClass; per-class cap on batch_size + max_wait_ms. should_flush(now, pending, oldest_at) returns true when the batch should be sent (full OR oldest aged out)."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
