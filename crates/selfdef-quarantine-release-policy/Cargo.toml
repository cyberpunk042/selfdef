[package]
name = "selfdef-quarantine-release-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS gate for releasing a quarantined event back into the bus. classify(reviewed_by_count, min_age_ms, age_ms, has_operator_signoff) returns Releasable / NeedsMoreReviewers / TooFresh / NeedsOperator. Pure decider; pairs with selfdef-quarantine-engine (the queue itself)."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
