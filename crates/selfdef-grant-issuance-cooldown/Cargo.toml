[package]
name = "selfdef-grant-issuance-cooldown"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS per-grant-template cooldown between successive issuances of the same template id. record_issued(template_id, ts) records; classify(template_id, now) returns Ready / Cooldown{ready_at}. Distinct from selfdef-mode-cooldown-policy (mode flip) and selfdef-policy-cooldown-window (policy edits) — this is the grant-issuance lane."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
