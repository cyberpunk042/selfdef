[package]
name = "selfdef-circuit-breaker-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS circuit-breaker authority. Per-subject 3-state (Closed/Open/HalfOpen) breaker with sliding-window failure threshold + open_for_seconds timeout. record_outcome/probe drives transitions; admit gates requests. Distinct from rate-limit (which throttles successful calls); this trips on failures."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
