[package]
name = "selfdef-policy-delta-feed"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS append-only feed of policy delta events. push(policy_id, kind, version_after, ts); since(cursor) returns events with seq > cursor + new cursor. Pure descriptor; subscribers (mirrors, dashboards) pull deltas by cursor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
