[package]
name = "selfdef-policy-graceful-drain"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS graceful-drain tracker for active decisions during policy reload. begin_drain(now, deadline_ms) sets the drain window; observe(in_flight_count, now) returns DrainContinue/DrainReadyToSwap (count reached zero or deadline passed). Pure descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
