[package]
name = "selfdef-mode-transition-authority"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side authority over ExecutionMode transitions — owns the dangerous-transition matrix (which from->to pairs require an explicit operator direct-shift acknowledgement, which require a snapshot, which are forbidden outright). The runtime mode-transition log mirrors approved transitions; this crate refuses any that fail authority."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
selfdef-execution-mode-policy = { path = "../selfdef-execution-mode-policy" }

[features]
default = []
