[package]
name = "selfdef-policy-namespace-quota"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS per-namespace cap on the number of policies that can live in a namespace at once. plan_install(ns, current_count) returns Accepted/CapReached{cap}/UnknownNamespace. Pairs with selfdef-policy-namespace-policy (hierarchical namespace tree) — this is the count-cap lane."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
