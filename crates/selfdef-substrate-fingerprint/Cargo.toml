[package]
name = "selfdef-substrate-fingerprint"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side substrate fingerprint snapshot — boot-time hash-summary of (rule_pack_semvers, doctrine_text_hash, collector_kinds, host_fingerprint). Lets the daemon detect tampering / drift between boots by comparing the fresh snapshot to a pinned reference."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
