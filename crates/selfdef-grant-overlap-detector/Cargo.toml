[package]
name = "selfdef-grant-overlap-detector"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side grant overlap detector — given a list of active GrantEntry, flags pairs whose (kind, scope) overlap (e.g. two Filesystem grants where one scope is a prefix of the other). Operator dashboard surfaces these so duplicates can be cleaned up."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
selfdef-grants-mirror = { path = "../selfdef-grants-mirror" }

[features]
default = []
