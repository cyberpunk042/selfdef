[package]
name = "selfdef-trace-id-issuer"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side trace_id issuer — deterministic trace_id minting + uniqueness assertions. Format: tr-{nano-epoch}-{sequence}-{actor-hash16}. The daemon mints every trace_id through this issuer; uniqueness is enforced via a sliding-window cache to detect collisions immediately."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
