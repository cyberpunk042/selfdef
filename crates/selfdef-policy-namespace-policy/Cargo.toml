[package]
name = "selfdef-policy-namespace-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS policy-namespace registry. Each namespace declares (id, parent_id, mutable). resolve(ns, key) walks the namespace tree from leaf to root for inherited policies. Mutability prevents operator hands from poking sealed namespaces."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
