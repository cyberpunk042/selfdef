[package]
name = "selfdef-rule-pack-version"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side rule-pack version manifest — the daemon loads N versioned rule packs (filesystem / network / capability / sandbox / communication / collector-budget / quarantine / commit-authority); each pack carries (id, semver, MS003 signature, loaded_at). Refuses boot if any pack is unsigned or older than the pinned floor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
