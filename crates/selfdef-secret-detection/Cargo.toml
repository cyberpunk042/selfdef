[package]
name = "selfdef-secret-detection"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side secret detection — heuristic scanner over outbound payload strings for high-entropy + known-format secrets (AWS access keys / GCP service-account JSON / GitHub tokens / generic API keys). Blocks egress when match score >= threshold."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
