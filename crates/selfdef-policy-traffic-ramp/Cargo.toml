[package]
name = "selfdef-policy-traffic-ramp"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Per-policy traffic ramp: deterministic actor-bucket gating by basis-point percentage."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
