[package]
name = "selfdef-llm-context-shrink-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS policy for which LLM context messages to drop first when the context window is over budget. plan_shrink(messages, over_by_tokens) returns a Vec<DropAction> per message (Keep/Summarize/Drop). Strategy: never drop system; drop oldest scratch first, then user turns, never the most recent N. Pure descriptor; the orchestrator applies."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
