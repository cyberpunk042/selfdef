[package]
name = "selfdef-burst-detector"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS short-window burst detector per (subject, kind). observe(subject, kind, ts) records; classify(subject, kind, now) returns Calm/Elevated{count}/Burst{count, threshold} based on count-in-window thresholds. Pure descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
