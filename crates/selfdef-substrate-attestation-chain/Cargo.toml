[package]
name = "selfdef-substrate-attestation-chain"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS substrate attestation chain. Each AttestationEntry asserts a (component, version, fingerprint) tuple was observed at a wall-clock instant; entries are chained with FNV-1a links so any tampering shows as a broken chain. Verification walks the chain from genesis; the first broken link is reported with index + expected vs actual hash."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
