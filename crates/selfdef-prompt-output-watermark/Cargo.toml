[package]
name = "selfdef-prompt-output-watermark"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS prompt-output watermark embedding. Embeds an invisible per-decision marker (zero-width chars) into LLM outputs so an operator can later prove a piece of text was produced by this engine instance. verify(text) attempts to recover the embedded fingerprint."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
