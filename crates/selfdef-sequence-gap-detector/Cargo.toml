[package]
name = "selfdef-sequence-gap-detector"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Detects gaps in a monotonic sequence-number stream."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
