[package]
name = "selfdef-grant-coverage-summary"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side per-subject grant coverage rollup — given a list of GrantEntry (mirror format), produces a per-(subject, GrantKind) count + total TTL remaining + earliest-expiry timestamp. Cockpit reads this for the per-subject grant chip cluster."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
selfdef-grants-mirror = { path = "../selfdef-grants-mirror" }

[features]
default = []
