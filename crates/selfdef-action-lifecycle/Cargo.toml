[package]
name = "selfdef-action-lifecycle"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS action lifecycle state machine. Phases: Requested → Approved → Executing → (Completed | Failed | Cancelled). transitions enforced; bad transitions error. Per-action timestamps recorded for each phase entry."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
