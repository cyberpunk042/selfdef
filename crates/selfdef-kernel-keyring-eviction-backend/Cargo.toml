[package]
name = "selfdef-kernel-keyring-eviction-backend"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "SDD-076 MS1 — kernel-keyring eviction backend trait + in-memory test impl. Twelfth IPS enforcement primitive extending undectet → duodectet at the kernel-keyring axis (pairs with SDD-068 token-revoke + SDD-069 MFA-grant + SDD-074 env-scrub + SDD-075 capability-drop at the credential-axis family)."
license = "MIT OR Apache-2.0"

[dependencies]
async-trait = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }
serde_json = { workspace = true }
