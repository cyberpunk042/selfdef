[package]
name = "selfdef-warmup-ramp"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Linear cold-start capacity ramp-up over a warmup window."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
