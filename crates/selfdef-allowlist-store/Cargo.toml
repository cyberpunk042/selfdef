[package]
name = "selfdef-allowlist-store"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "TTL'd allowlist with audit reason for explicit exemptions."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
