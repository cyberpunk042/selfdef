[package]
name = "selfdef-policy-cas-store"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Compare-and-swap policy value store with monotonic per-key versions."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
