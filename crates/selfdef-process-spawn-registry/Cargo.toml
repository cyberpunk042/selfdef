[package]
name = "selfdef-process-spawn-registry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side process-spawn registry — operator-tracked registry of spawned subprocesses (pid, command, args, owner_subject, started_at). Used by the dispatcher to gate spawn calls and by the cockpit to render the per-subject process tree."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
