[package]
name = "selfdef-policy-bundle-pack"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side policy bundle pack — bundles together the rule packs + thresholds the operator wants to apply as a single named atom (e.g. 'baseline-2026-05-19', 'red-team-strict'). The daemon swaps packs atomically — either all rules update or none."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
selfdef-rule-pack-version = { path = "../selfdef-rule-pack-version" }

[features]
default = []
