[package]
name = "selfdef-event-emitter-rate-cap"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS per-emitter rate cap to prevent a single component from dominating the event bus. set_cap(emitter_id, events_per_min); record(emitter_id, ts) returns Accepted{remaining} / Throttled{cap, retry_after_ms}. Sliding 1-minute window. Pure ledger; distinct from selfdef-bus-priority-policy (priority order) and rate-limit-policy (Hz)."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
