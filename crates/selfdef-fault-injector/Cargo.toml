[package]
name = "selfdef-fault-injector"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Deterministic fault-injection policy by tag + rate basis points."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
