[package]
name = "selfdef-namespace-precedence"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS namespace precedence resolver. Each namespace declared with a numeric priority; resolve(namespaces) returns the namespace with the highest priority (deterministic tiebreaker = lexicographic id). Pairs with selfdef-policy-namespace-policy (hierarchical tree) — this is the rule-priority lane."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
