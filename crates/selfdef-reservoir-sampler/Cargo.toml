[package]
name = "selfdef-reservoir-sampler"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Reservoir sampler — uniform random sample of K items from a stream of N."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
