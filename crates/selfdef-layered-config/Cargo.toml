[package]
name = "selfdef-layered-config"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Layered config — top layer overrides; lookup returns first match."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
