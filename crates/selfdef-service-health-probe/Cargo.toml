[package]
name = "selfdef-service-health-probe"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Per-service health probe with hysteresis (consecutive success/failure thresholds)."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
