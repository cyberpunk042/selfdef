[package]
name = "selfdef-monotonic-counter"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Monotonic counter — advance-only u64 with rollback rejection."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
