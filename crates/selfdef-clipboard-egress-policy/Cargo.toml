[package]
name = "selfdef-clipboard-egress-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS clipboard egress policy. Gates whether content of a given ContextSensitivity class may leave the engine via clipboard, into which target surfaces (local-only / cross-profile / external-paste-buffer), and under which profile. Top-Secret never reaches clipboard; Confidential requires operator approval per copy event."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
