[package]
name = "selfdef-ingest-admission-gate"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Ingest-time admission checks (origin, size, kind) for inbound events."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
