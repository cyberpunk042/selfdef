[package]
name = "selfdef-process-launch-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS process-launch authority. Gates which binaries (by absolute path or shell name) the engine may spawn, with per-binary allowed argument prefixes (e.g., `git status` allowed, `git push --force` denied) and an explicit deny-list (deny wins). NeverLaunch hard set always denies even under wildcards."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
