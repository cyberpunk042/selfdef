[package]
name = "selfdef-mode-pre-flight"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side pre-flight check for ExecutionMode entry — verifies (snapshot fresh / eval-suite recent / audit log healthy / bus subscribers wired / boundary policies signed). The daemon refuses mode entry on any failure."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
selfdef-execution-mode-policy = { path = "../selfdef-execution-mode-policy" }

[features]
default = []
