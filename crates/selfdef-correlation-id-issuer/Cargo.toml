[package]
name = "selfdef-correlation-id-issuer"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Request-scope correlation IDs with parent linkage and lineage walk."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
