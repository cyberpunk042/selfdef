[package]
name = "selfdef-decision-throttle"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side per-subject decision throttle — token-bucket rate gate. Each subject has a bucket of `bucket_capacity` tokens refilling at `refill_per_second`. Refuses a decision if the bucket is empty."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
