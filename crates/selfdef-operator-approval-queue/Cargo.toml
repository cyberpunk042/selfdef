[package]
name = "selfdef-operator-approval-queue"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Operator approval queue with priority, deadlines, and decision recording."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
