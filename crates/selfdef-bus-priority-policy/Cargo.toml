[package]
name = "selfdef-bus-priority-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side event bus priority policy — assigns a priority class (Critical / High / Normal / Low) per event-class so that during fan-out budget pressure, critical events are delivered first. Composes selfdef-bus-subscriber-registry."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
