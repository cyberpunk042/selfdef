[package]
name = "selfdef-prompt-allowlist-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS prompt-template allowlist. Each LLM call must reference a template_id that has been registered. unknown ids are rejected (no implicit prompts). Templates carry version + parameter-name schema so callers can be matched against the declared shape."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
