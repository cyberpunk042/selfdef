[package]
name = "selfdef-network-egress-decision"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side network egress decision — given an outbound URL or socket address + active subject + active grants, decides Allow/Deny/Ask. Composes selfdef-policy-decision Outcome; the daemon emits one decision per egress attempt."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
selfdef-policy-decision = { path = "../selfdef-policy-decision" }

[features]
default = []
