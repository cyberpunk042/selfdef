[package]
name = "selfdef-tool-cancellation-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS tool-cancellation authority. Each tool declares its CancelMode (Never/SafeOnly/Anytime). cancel(tool_id, in_flight_phase) returns Allow / Denied. SafeOnly tools only accept cancel during 'prepare' phase, not mid-IO; Anytime accepts always; Never refuses every cancel."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
