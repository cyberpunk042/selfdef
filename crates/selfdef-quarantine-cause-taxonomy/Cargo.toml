[package]
name = "selfdef-quarantine-cause-taxonomy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side quarantine cause taxonomy — 8 canonical causes (PolicyViolation / SchemaInvalid / HardEpsBreach / AnomalyTriggered / DriftDetected / UnsignedRequest / UntrustedSubject / ManualOperator) each carrying severity floor + auto-clear / requires-operator-clear semantics."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
selfdef-incident-classifier = { path = "../selfdef-incident-classifier" }

[features]
default = []
