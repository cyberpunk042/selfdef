[package]
name = "selfdef-peak-detector"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Rolling-window peak (max) detector over i64 samples."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
