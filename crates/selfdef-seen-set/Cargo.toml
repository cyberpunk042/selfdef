[package]
name = "selfdef-seen-set"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Bounded seen-id set with TTL — first-time detector."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
