[package]
name = "selfdef-network-cidr-allowlist"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "IPv4 CIDR allowlist matcher for network egress/ingress decisions."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
