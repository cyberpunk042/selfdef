[package]
name = "selfdef-policy-version-pin"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS per-actor policy-bundle version pin. pin(actor, bundle_id, version). resolve(actor) returns Pinned{bundle_id, version} or Unpinned. Useful for staged rollout where particular actors stay on prior bundle versions while the rest migrate."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
