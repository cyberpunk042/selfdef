[package]
name = "selfdef-evidence-search"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "IPS-side evidence-ledger search — filter by tag + subject + time-range over a list of EvidenceLedgerEntry. Returns paged hits the operator can iterate. Companion to selfdef-evidence-ledger + selfdef-evidence-tag."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
selfdef-evidence-tag = { path = "../selfdef-evidence-tag" }

[features]
default = []
