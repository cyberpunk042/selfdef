[package]
name = "selfdef-api-token-revocation-backend"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "SDD-068 MS1 — API/web-token revocation backend trait + in-memory test impl. The BusEventBackend production adapter ships in MS1b under the `bus-event-backend` feature; this crate hosts the contract + InMemoryBackend per the MS1-substrate decision."
license = "MIT OR Apache-2.0"

[dependencies]
async-trait = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }
serde_json = { workspace = true }
