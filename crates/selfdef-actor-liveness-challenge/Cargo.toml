[package]
name = "selfdef-actor-liveness-challenge"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Issue + verify single-use liveness challenges with reply windows."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
